# DESARROLLO_CUADERNO — léeme ANTES de editar el cuaderno

Objetivo del cuaderno: que alguien **totalmente ajeno entienda MADRE en <60s** y que se sienta como una
**criatura viva y explorable**, no documentación. Tony quiere: mucha interacción, cosas locas/graciosas/
llamativas, MUY explicativas, easter-eggs y recompensa por explorar — **sin romper el orden ni la comprensión**.

## ✅ Checklist antes de tocar `index.html`
1. `git pull` y edita SOLO el `index.html` del repo (fuente única).
2. Tras editar, VALIDA: `node --check` de cada `<script>` ejecutable + del módulo; estructura (`<script>`==`</script>`, cierra `</body></html>`).
3. VERIFICA EN NAVEGADOR (preview local con `python -m http.server`): que la pieza funciona y **0 errores de consola**. Para Supabase, prueba contra la BD real y BORRA filas de prueba.
4. Añade una entrada al array `cambios` (Evolución) y línea al `CHANGELOG.md`.
5. Actualiza este doc con cualquier lección nueva.

## ⚠️ Lecciones / gotchas (errores ya cometidos — no repetir)
- **`mask-image` + `mask-composite` (esquinas rotas)**: frágil entre navegadores; dejó la nota "rara". NO usar para recortes; usar `border-radius` irregular.
- **`backdrop-filter:blur` en un overlay a pantalla completa** deja BORROSA también la zona que quieres destacar. Para spotlight usa el truco `box-shadow:0 0 0 9999px rgba(0,0,0,.6)` SOBRE el elemento destacado (su interior queda nítido) y backdrop transparente.
- **Selectores de "señala una zona" (lápiz/corazón)**: incluir TAMBIÉN cabecera y bordes (`h1, .sub, .postit, #gateBar, footer, .heronote, .dec, .alert, .step, .chip, #senalesCard, #comentarios`), no solo `.card/.hero`.
- **Barras fijas (banners de modo)**: ponles `pointer-events:none` o tapan/bloquean el contenido que el usuario quiere tocar.
- **TSV horneado**: preservar TABULADORES (usar Python, no edición a mano).
- **Python en consola Windows (cp1252)**: `print()` con emojis PETA → usa `PYTHONIOENCODING=utf-8` o evita emojis en prints.
- **Texto de usuario SIEMPRE con `textContent`** (anti-XSS), nunca `innerHTML` con datos.
- **`window.prompt` no se puede "interceptar"** para reacciones (p.ej. contraseña que se ríe) → usar modal propio.
- Widgets nuevos: IIFE autocontenido con `try/catch`, prefijo de clases propio, position:fixed/append-only, móvil OK, usar globales `window.__sb` (Supabase, puede tardar→poll), `window.__fb`, `window.__openFb`.
- **Gato pegado al borde superior**: el gato vive en los bordes; si su `y` mínima es ~14px se **tapa con la barra del navegador/notch**. Solución: `TOPSAFE` (≈64px, leído de `env(safe-area-inset-top)`) como `y` mínima en spawn y clamp.
- **Deshacer en `<canvas>`**: pila de `getImageData/putImageData` en **píxeles de dispositivo** (`canvas.width/height`, que ya incluyen DPR); `putImageData` ignora el `scale(DPR)` del ctx, así que va en 0,0. Snapshot al EMPEZAR cada trazo (`pointerdown`) + en borrar. Atajo Ctrl/Cmd+Z global **ignorando** si el foco está en `INPUT/TEXTAREA/contentEditable`.
- **Limpiar al enviar NO basta si hay deshacer (bug de doble envío, v0.8)**: el dibujo se enviaba, se hacía `clearCanvas()`… pero la pila de deshacer seguía intacta → Ctrl+Z recuperaba lo recién enviado y se podía **REENVIAR el mismo** N veces. Fix: al enviar con éxito, **vaciar la pila de deshacer** (`undoStack.length=0; refreshUndo()`) + guarda **`isBlank()`** (lee alpha del `getImageData`; si no hay píxeles, no envía). Lección general: cualquier widget que "limpia al enviar" pero mantiene historial/undo puede reenviar lo mismo; vacía el historial en el éxito.
- **Contador real de visitas**: localStorage NO sirve (solo cuenta tu navegador). Hecho con Supabase: tabla `public.counters(key,n)` + RPC `bump_counter(k)` **SECURITY DEFINER** (incrementa atómico, sortea RLS), `grant execute … to anon`. Bump 1×/sesión con `sessionStorage` (un refresco no infla). Empieza en 0 a propósito (ética MADRE: "mide el mundo").
- **RLS y métricas públicas**: `feedback` es PRIVADA (anon no la lee) → un `count` sobre ella siempre da 0 en la web. Para stats públicos usar `comments`/`drawings` (sí legibles), p.ej. "respuestas de MADRE" = `comments` con `ai_replied=true`.
- **Caos en móvil**: detectar `(max-width:640px)` y `prefers-reduced-motion` → factor `K` que recorta ~70% partículas y alarga intervalos. Limpiar SIEMPRE con una clase común (`.chaos-el`) al apagar.
- **Scripts `type=module` son DIFERIDOS**: corren DESPUÉS de los `<script>` clásicos inline. Si un widget clásico (p.ej. el **HUB de FAB**) depende de algo creado en el módulo (el FAB de comentarios `#cmtFab`), inicialízalo en `DOMContentLoaded` (cuando los módulos ya corrieron), no en línea. Si no, "desaparece" ese FAB. *(Bug real cazado por la review adversarial.)*
- **HUB de FAB seguro**: el hub oculta los FAB sueltos con `body.fabhub`; **añade esa clase SOLO al final**, tras construir el menú con éxito (dentro del try). Así, si el hub peta, los FAB sueltos siguen visibles (no se pierde acceso). El menú **proxya** `real.click()` (no recrea listeners) y refleja `.on` leyendo el FAB real.
- **`getBoundingClientRect()` y `translate3d`**: con elementos movidos por `transform:translate3d` en capa compuesta, el rect puede leerse 0,0 en algún motor (p.ej. el preview headless). Para verificar posición, leer `style.transform`, no el rect (pasó con el gato `#gxg-cat-root`).
- **Gato en móvil**: no hay `mousemove` (solo se registra en no-touch). El `touchstart` debe ser **passive y SIN `preventDefault`** (si no, atrapa el scroll = "se atasca"). Y `edgePos` debe **evitar las esquinas inferiores** (donde viven los FAB) para no quedarse encima.
- **Chips de cabecera**: calcular del estado real (`refreshHeaderChips`: cuenta `.dec` no respondidas + `desvios.length` + sev `r`→salud "atención"). OJO: `desvios` se declara DESPUÉS del primer `refreshDecScore()` → guardar con `typeof desvios!=='undefined'` y re-llamar tras `renderDesvios()`.
- **Identidad anónima** (`window.__identity`, clave `cm_identity_v1`): alias persistente que pre-rellena nombre de dibujos/comentarios (vía `focusin` para formularios creados dinámicamente) + badge "· tú" (guarda ids de dibujos subidos; el insert usa `.select('id').single()`). Dueño verificable-para-todos = backend (diferido).
- **ASMR sin ficheros**: Web Audio sintetizado (oscilador + ruido filtrado) para pop/click/creak. `AudioContext` perezoso (autoplay necesita gesto), `suspend` en `visibilitychange`, hover solo si `(hover:hover)` y no `reduced-motion`. Control en el HUB (`window.__asmr`).
- **Validación de imagen (cliente)**: `shrinkImg` (canvas → JPEG ≤1600px, q .82) + rechazo de no-imagen/>8MB en `uploadImg`. NO es seguridad (bypasseable) → el control real es backend (parking lot).
- **`requestAnimationFrame` se PAUSA si la pestaña no está visible**: si usas rAF para "encender" una clase de entrada (fade-in), en una pestaña oculta o en el preview headless NO se aplica → la pieza parece no aparecer. Para clases de estado que deben aplicarse sí o sí, usa `setTimeout(...,20-30)`, no rAF (pasó con el globito `#tourNudge` del latido de la ruta guiada). rAF está bien solo para animación puramente visual que da igual si se pausa con la pestaña.
- **Insertar elementos "entre paneles" sin romper anclajes**: las texturas ASMR se insertan como `<aside class="cmTex">` ENTRE las `.card` (que son `<div>`). Se usa `<aside>` a propósito: el anclaje de los corazones 💗 (`cssPath`) usa `:nth-of-type` POR TAG, así que meter `<aside>` no cambia el `nth-of-type` de los `<div>`/`<p>`/`<li>` → los corazones guardados siguen cayendo en su sitio entre sesiones. Si hubiera usado `<div>` para las texturas, habría descolocado los corazones.
- **ASMR ON por defecto, pero respetando al usuario**: `cm_asmr_v1` ahora arranca `enabled=true` salvo que haya `"0"` guardado (alguien que lo apagó). El audio del navegador NO suena hasta el primer gesto → `unlock()` (pointerdown/keydown/touchstart, `{once}`) hace `ensure()+resume()`; las cosas que se clican (burbujas) ya sirven de gesto. Sonido = motor sintetizado: `brush(speed)` (fricción por velocidad del puntero), `pop`/`crack`/`crunch`.
- **Verificar en navegador con Supabase Realtime → el screenshot por "networkidle" NUNCA termina** (el websocket queda abierto): no es un cuelgue de la web. Verifica por `preview_eval` (estado del DOM, flujos) + `preview_console_logs` (0 errores), no por captura. Para una captura puntual habría que cortar el realtime antes.
- **Verificar widgets visuales en el preview headless → el viewport ARRANCA con ancho 0**: `window.innerWidth` puede ser 0 y entonces TODO mide ~0 (un SVG con `width:100%` sale 0×0 y PARECE roto, pero no lo está). Prueba diagnóstica: una tarjeta que YA funcionaba (`#senalesCard`) mide igual de diminuta → es el viewport, no tu pieza. Antes de medir tamaños/render, **fija el viewport con `preview_resize`** (mobile/desktop). Para evidencia: `getBBox()` (geometría interna, no depende del layout) + `getBoundingClientRect()` YA con viewport real (a 375px el SVG del widget «dónde vive» dio 315×431, aspecto correcto, sin desbordar). *(Casi subo un widget «roto» que en realidad estaba bien — rule 1b me salvó.)*
- **Reescribir funciones del motor pero dejar llamadores viejos = fallo silencioso**: al cambiar el ASMR (eliminé `pop()`/`crack()`/`crunch()` y metí `brush(prof,speed)`/`hit(prof)`), el `buildRibbons` viejo seguía llamando a `crunch`/`crack` → `ReferenceError` a mitad del bucle (tragado por el `try/catch` del IIFE) → **solo se creaba 1 ribbon** y con tipo `null`. Síntoma: "antes salían N, ahora 1". Lección: al cambiar una firma/quitar una función, **busca TODOS los llamadores** (grep) en la misma pasada; y un `try/catch` que envuelve un bucle de construcción **oculta** que petó pronto (cuenta los elementos creados como verificación).
- **Corpus de contenido como data-island** (`window.__cmCorpus`, `<script id="cmCorpus">`): generado por un workflow de agentes (plantillas SVG, datos, órdenes, comentarios, emoji-labels, skins), validado (SVG: solo tags permitidos, sin `<script>`/`on*`) y embebido como objeto JS (JSON es subconjunto válido de JS). Lo leen varios widgets. Va al principio del `<body>` para estar disponible antes que ellos. Las plantillas se guardan SIN `fill/stroke` por elemento; el estilo se aplica con un `<g fill="none" stroke="#b9aef0"…>` al rasterizar (más compacto).
- **Rasterizar SVG a `<canvas>`**: `new Image()` con `src='data:image/svg+xml;charset=utf-8,'+encodeURIComponent(svg)` y `ctx.drawImage`. Para que cargue, el `<svg>` necesita `xmlns` y `width/height`.
- **Plantilla/guía que NO debe salir en el resultado** (bug v0.7→fix v0.7.1): si la "horneas" en el lienzo (`drawImage`), queda en la imagen enviada (Tony: "deja un resto"). Solución: **guía superpuesta** = `<img id="dbjsGuide">` aparte (z bajo, `pointer-events:none`, opacidad ~.55), lienzo **transparente** (`clearRect`, no `fillRect` blanco; el blanco lo pone el `wrap`), y al exportar **componer** un canvas nuevo `fillRect blanco + drawImage(lienzo)` → sube blanco+trazos SIN la guía. Verificable: tras aplicar plantilla, `getImageData` del lienzo sigue con alpha 0 (no se pintó nada).
- **Modo "señalar" (nieSelect) y modos exclusivos**: el clic de selección se captura en **fase de captura** (`addEventListener(...,true)`) + `stopPropagation` para que NO dispare otros handlers; salir con Esc o clic fuera. Mismo patrón que lápiz/corazón.
- **Recorte de imagen (`cropImg`)**: modal con caja arrastrable (`pointerdown` en la caja=mover, en el handle=resize; `pointermove`/`pointerup` a nivel document QUE SE QUITAN en `cleanup()`). Inyectado en `uploadImg` ANTES de `shrinkImg`; devuelve File recortado, el original ("usar entera") o `null` (cancelar→aborta subida). Recorte real a resolución natural (canvas) y luego `shrinkImg`. Sigue sin ser seguridad (es cliente).

## 🎨 Fuentes de inspiración (para sacar ideas de UI viva)
Galerías de COMPONENTES ya hechos (lo más útil, copiar HTML/CSS):
- **UIVerse** (uiverse.io) — miles de botones/cards/toggles/hover; buscar: cute, playful, paper, note, pixel, cartoon.
- **Animista** (animista.net) — genera CSS de entradas/rebotes/flotación (ideal post-its, gato, notas).
- **Hover.css** (ianlunn.github.io/Hover) — efectos hover (rebotes, inclinaciones, pulsos, vuelos).
- **LottieFiles** — animaciones: cat, paper plane, pencil, sticker, note, mail.
- **React Bits / Magic UI / Aceternity** — microinteracciones (inspiración aunque usemos HTML puro).
Mecánicas e ideas:
- **Codrops** (tympanus.net/codrops) — efectos/transiciones experimentales.
- **CodePen** — buscar: floating cat, sticky note, paper airplane button, pixel character, hand drawn ui.
- **Neal.fun** — cómo explicar cosas complejas con interacción y curiosidad (muy alineado con MADRE).
- **GSAP showcase**, **Awwwards (experimental/interactive)**, **Godly**, **Land-book**, **itch.io / OpenGameArt** (sprites pixel gratis).
Términos de búsqueda: "playful web design", "delightful ui", "microinteractions inspiration", "whimsical website",
"interactive easter egg website", "sticky note ui", "paper note animation", "notion/figma comments ui",
"pixel art website character", "gamified feedback".

## 🔭 Plan: rutina "buscadora de ideas" (FUTURO — aún no montada)
Una tarea programada que: (1) navegue/observe estas fuentes (WebFetch/búsqueda), (2) extraiga 1-3 mecánicas
reutilizables (snippet CSS/JS), (3) las proponga en `PENDIENTE_TONY.md` (o un `IDEAS_UI.md`) etiquetadas
con impacto/encaje, sin tocar la web sola (Tony elige). Encaja con la naturaleza N4 de MADRE (mejorar la
capacidad de mejorar). Persistido aquí para construirla más adelante.

## 🗂️ Backlog (ideas pedidas, AÚN no hechas — persistidas)

> Norte (de Tony/ChatGPT): que la web **actúe como MADRE** (deja notas, cambia cosas, esconde,
> recuerda, genera) en vez de solo explicarla → sensación de **"estoy explorando algo vivo y cambiante"**.
> Componentes visuales candidatos viven en **`MADRE_UI_REFERENCIAS.md`**.

**Prioridad alta (criterio de Tony):**
1. **Comentarios anclados VISIBLES para todos** (estilo Notion/Figma): hoy el lápiz captura la zona y la manda a MADRE; falta que el marcador/comentario quede pegado y visible para cualquier visitante (anclaje robusto + render al cargar). *El de más impacto.*
2. **Fotos/avatares** en comentarios: avatar aleatorio de un set, o que cada quien suba la suya. (Ya hay imagen adjunta.)

**Ideas "organismo vivo" (de la lista de ChatGPT que Tony aprobó) — por hacer:**
3. **El gato coleccionista**: 1 gato escondido que cada día aparece en un sitio distinto; al encontrarlo "has encontrado al gato nº17" y queda registrado (Supabase counter / localStorage). (Hoy el gato huye; falta la mecánica de "encontrar nº").
4. **La pared de garabatos**: los dibujos colgados como papeles reales (con likes/fecha/autor anónimo, algunos torcidos). Hoy hay galería simple; falta el "muro" físico.
5. **Mensajes encontrados**: trocitos de papel por la página ("hoy pensé algo raro", "nota encontrada el 14 jun") que dan sensación de sistema vivo. (El modo caótico ya hace algo así temporal; falta versión permanente/escondida.)
6. **El botón inútil** ("no pulses"): cada pulsación hace algo distinto (gato, nota que cae, sonido, frase absurda).
7. **El museo de ideas muertas**: sección con hipótesis abandonadas reales (da credibilidad y muestra evolución). Encaja MUCHO con MADRE; hoy hay "hipótesis muertas" mini → ampliar a sección-museo.
8. **El cementerio de bugs**: lista divertida ("☠️ Bug #14 — intenté pensar demasiado. Estado: eliminado").
9. **Huellas de visitantes**: "alguien estuvo aquí hace 3 min / dejó un dibujo hace 1 h" (parece habitado). Con la tabla `comments`/`drawings` + `created_at` es factible.
10. **El nivel secreto / recompensa al llegar al fondo**: "has llegado más lejos que el 90%" + desbloquea algo. (Liga con el **mapa de exploración**: visitas/llegaron-al-final/feedback/dibujaron — la parte "visitas" ya está en el panel 👁️.)
11. **Antorcha = "Modo explorador"**: encender la antorcha (ver `MADRE_UI_REFERENCIAS.md`) revela las notas/secretos ocultos.

**Otros:**
12. **Coleccionables / polaroids de descubrimientos de MADRE / pegatinas** que dejan visitantes.
13. **Gato: barriguita completa** ("me he cansado de huir, acaríciame" → enseña barriga, ronronea) y que se siente sobre paneles.
14. La **rutina buscadora de ideas** de UI (sección 🔭 de arriba).

**Hecho (tanda 1, 2026-06-21):** contador de visitas 👁️ + panel "El cuaderno por dentro" (lo más
querido + actividad + "lo que opina MADRE" = mapa-de-exploración y máquina-de-pensamientos en versión
lite) · modo caótico narrativo (flechas/notas/avisos absurdos + recorte móvil) · arreglado gato tapado ·
deshacer (↩️/Ctrl+Z) en dibujos. Favoritos de Uiverse guardados en `MADRE_UI_REFERENCIAS.md`.

**Hecho (tanda 2, 2026-06-21):** **HUB de FAB** (los 6 botones flotantes en un menú ✦ + nudge "toca aquí") ·
**ASMR** (sonidos sintetizados en hover/click, toggle en el hub) · botones **"🤔 no entiendo"** por tarjeta y
en la ruta guiada (con aburre/me gusta/desarrollar) · gato **cambia de pose** + arreglo móvil (no atasca) ·
**modo caótico x2** (flechas que apuntan a elementos reales, círculos, citas de comentarios reales, megaFlip,
travesuras contextuales) · chat abre **en lo último + foco** · comentarios in-page con **scroll interno** ·
post-it **más claro** (chincheta/borde) · dato **"MADRE = carpeta con identidad"** · **dibujos con dueño**
(identidad anónima + badge "tú") · **modos exclusivos** (no se solapan barras) · **bugs**: chips reales
(5 decisiones), aviso de estado **desfasado >48h**, `.limit()` en queries, validación/resize de imagen,
tema **por día**, metas **Open Graph**, `aria-live` en toasts. Nuevo doc **`DISEÑO_HEURISTICAS.md`** (cómo
diseñar: "de la abuela al borracho", checklist, patrones, reutilizar para webs personalizadas).

**Hecho (v0.7, 2026-06-22):** ASMR **motor reescrito** (suena mejor) + **8 texturas que rotan por carga sin repetir** ·
**plantillas de dibujo** (24, corpus generado por workflow) para calcar · **"no entiendo → señalar la parte"** ·
caos con **clickbait + datos random + órdenes/retos + más comentarios** (corpus) · tour con **paso sorpresa = dibujos** ·
**etiquetas de emoji** (title/aria) · **skins de panel** (variedad por día) · **comentarios en burbujas** + mini-avatar ·
**recorte de imagen** al subir (`cropImg`) · HUB: lo encendido sube arriba · **versión visible** + nueva **`GUIA_FACIL.md`**.

**STAGED — pedido por Tony (2026-06-22) pero NO hecho aún (plan listo para la próxima vuelta):**
- **Estado VIVO de verdad (no horneado)** *(prioridad de Tony, "empiezo por el 1")*: mover `global_score/areas/desvios/senales/investig/hipotesis/changelog` a una tabla Supabase; el loop hace `upsert` (service_role) cada pasada; el cliente `select` + se suscribe al canal (igual que `comments`). Cambio aparece al instante, sin esperar build. **Riesgo:** requiere editar el SKILL del loop `cuaderno-feedback` para que escriba (verificable solo con Tony en la app) → hacerlo con cuidado o dejar nota `DESDE_CLAUDE_*`.
- **Cerrar el ciclo de decisiones:** estado por `.dec` (pendiente/procesada/aplicada) que el loop actualiza + badge en la tarjeta. Depende de la tabla de arriba.
- **Corazones/likes alrededor de las zonas MÁS queridas + comentarios alrededor de las MENOS entendidas** (Tony): hoy el corazón 💗 es local (lo tuyo). Para que sea GLOBAL hace falta una tabla pública `zone_signals(zone_key, likes, confusions)` + RPC `bump_zone` SECURITY DEFINER (patrón `counters`), que el cliente incremente al marcar gusto/no-entiendo y lea para pintar corazones/notas alrededor. (Las confusiones reales como texto flotante = otra tablita pública o reusar el `section`.)
- **Subir VÍDEO + recortar (dimensiones y inicio/fin):** nuevo (hoy solo imagen). Cliente: `<video>` + dos sliders (trim) + `MediaRecorder`/canvas para reencodear (pesado). Empezar por trim de tiempo; recorte de dimensiones después.
- **`og:image` 1200×630 real** (PNG): la web no tiene preview al compartir. (SVG-og lo soportan pocas plataformas → hace falta rasterizar a PNG.)
- **Gestor único de timers + `requestAnimationFrame`** (gato/ASMR/nudge corren desde el segundo 1): consolidar para ahorrar batería en móvil. **Riesgo:** refactor transversal.
- **Llevar a tierra TODO el copy in-page** (abuela↔borracho) + revisar "por dónde van los avances": pasada de simplificación de textos de tarjetas (grande). Empezado el marco en `GUIA_FACIL.md`.
- **Toggled-ON fuera del colapsable (no solo arriba):** hecho a medias (en el HUB lo encendido sube arriba del menú). Confirmar con Tony si quiere los activos como chips SIEMPRE visibles fuera del menú.

**PARKING LOT previo (sigue pendiente — detalle en `DISEÑO_HEURISTICAS.md` §7):** **SEGURIDAD real (prioridad nº1)**
(rate-limit/RLS por IP/honeypot/contraseña fuera de texto plano); comentarios anclados visibles para todos;
split de `index.html`; localStorage namespaced+versionado; validación de imagen también en feedback;
modal custom para `confirm`/`prompt`; usar **laterales** en escritorio.
**Splice:** Tony lo conectó; `describe_a_sound` (buscar) es gratis, `download_asset` **gasta créditos** y pide
confirmar + hay duda de licencia para incrustar en web pública → de momento ASMR es sintetizado; presentar
candidatos de Splice a Tony antes de descargar.

## 🗂️ STAGED v0.11+ — backlog «organismo vivo» pedido por Tony (28-jun, tanda larga) — PERSISTIDO, por hacer
> De la lista larga de Tony. **Norte:** que cada cosa diga «vive / aprende / sonríe» (ver `DISEÑO_HEURISTICAS.md` §9). HECHO ya en v0.11: onboarding, «no entiendo» por palabra, `#envivo` (latido+hámster+ánimo+«lo que no te cuento»), cementerio, plantillas, caos sin temblor, comentarios, brillo secreto. **Lo que FALTA:**
- **Cablear las frases del GATO a contextos** (ya REDACTADAS en el workflow `cuaderno-copy-vivo`, no integradas): scroll rápido / lleva rato inactivo / abrió todas las tarjetas / aparición genérica / **rarísimo 0.1%** (rompe la 4ª pared). Hoy el gato solo huye.
- **Feed de actividad VIVA / línea temporal del diario** («hace 8 min encontré un bug; hace 2 h descarté una idea…») — honesto (acciones reales: commits, comentarios, runs de rutinas).
- **Pensamientos en voz alta** (razonamientos reales, incluso los descartados) — tipo «❌ idea → por qué no».
- **Celebraciones automáticas en hitos REALES** (primer euro / primera estrella / primer usuario): confeti + el gato corre + sonido + entra en la historia. (Necesita un disparador de evento real.)
- **Memoria/evolución**: «hace 14 días pensaba X, hoy lo contrario»; «desde tu última visita cambié de opinión en N cosas» (localStorage de última visita).
- **Recompensa inmediata / predicción del visitante**: «¿MADRE logrará su 1er ingreso en 30 días? sí/no» (tablita pública o localStorage).
- **Huellas de visitantes** («342 pasaron, 112 dejaron huella» + huellas de colores que crecen) — usa `comments`/`drawings`/`counters`.
- **El mundo afecta a la web**: buen comentario → el gato lo dice al día siguiente; N estrellas → cambia una animación.
- **«Aging desk» / objetos con vida** (sutil): planta que crece, café que se enfría, lámpara que parpadea con actividad, papeles que se acumulan. Refuerza «escritorio de una IA».
- **Mostrar probabilidades**: «creo que esto funciona — 68%» y luego «me equivoqué». Da sensación de criterio, no de seguridad fingida.
- **«Cámara de vigilancia» «Sala del cerebro ● ONLINE»** (leyendo / pensando / descartando / durmiendo) — solo texto, pero parece que hay alguien dentro.
- **Coleccionables / badges** (localStorage, sin cuenta): «primer testigo», «estuviste antes del primer euro», «curioso profesional» (abrir todas las tarjetas).
- **Sorpresas escondidas (5, no 200)**: scroll muy rápido → el gato; 5 min dentro → «sigues aquí, gracias»; abrir todo → desbloqueo; clic 10× al gato → fallo histórico real.
- **«Detector de humo»** (🔥 medidor por idea nueva, que baja cuando MADRE la analiza).
- **Glitches mínimos** (palabra tachada, flecha dibujada, post-it nuevo) — «alguien pasó hace 5 min».
- **Separadores de scroll** («lo importante ya lo has visto; lo de abajo es para profundizar ↓») para que el scroll largo no asuste.
- **Pasada de simplificación de copy** (abuela↔borracho): que la MAYORÍA de tarjetas se entienda **sin** desplegar `▸ ver más`; reservar los desplegables para lo profundo. + **alternar la palabra «MADRE»** (el sistema / la IA / el proyecto) para que no canse.
- **Metáfora «ventanas»**: cada tarjeta = una ventana al organismo (cerebro / GitHub / gato / pasado / futuro / mundo). Dirección unificadora a futuro.

## 🌳🫁 STAGED v0.14+ — EL GRAN SALTO: DE «WEB CON COSAS VIVAS» A «ORGANISMO VIVO» (crítica grande Tony/ChatGPT, 29-jun) — NORTE ACTUAL
> **TESIS RECTORA (Tony, la mejora nº1 sobre todo lo demás):** convertir el cuaderno en una **HISTORIA, no una documentación**.
> Hoy el visitante APRENDE; queremos que además QUIERA VOLVER. La pregunta que debe quedarle al cerrar:
> **«¿Qué habrá conseguido MADRE la próxima vez que entre?»** Eso = razón para regresar, y vale más que otra animación/widget/sección.
> **Matiz clave:** el cuaderno es vivo TÉCNICAMENTE pero se PERCIBE como «una web que tiene cosas vivas», no como **un organismo que respira**.
> El salto no es más animaciones: es que **parezca que está ocurriendo algo mientras tú estás ahí** y que **siga viviendo aunque cierres la pestaña** («si vuelvo mañana, probablemente habrá cambiado algo»).

**A) RE-SECUENCIAR LA PRIMERA PANTALLA (el problema nº1: demasiadas cosas compiten; "si todo es importante, nada lo es").**
- Los primeros ~60 s deben responder SOLO **(1) ¿qué es?** y **(2) ¿por qué debería importarme?**. Todo lo demás, DESPUÉS.
- **Crear la PREGUNTA antes de enseñar la respuesta.** Hoy enseñamos capacidades (mapa, ramas, loop, widgets, hipótesis, pipeline, memoria…) antes de generar curiosidad → el visitante piensa «vale… ¿y esto para qué sirve?».
- **Menos texto (−~40%)** aunque esté bien escrito. Gancho de entrada tipo **«Estoy intentando construir una IA que mejore sola durante años»** (1ª persona, engancha) y dejar el «decide mejor / aprende de la realidad…» para MÁS TARDE.
- **El MAPA llega demasiado pronto: es una RECOMPENSA, no una introducción.** Moverlo a DESPUÉS de que se entienda MADRE.
- **Auditoría de funciones (carga mental):** comentar/dibujar/votar/feedback/explorar/mapa… por CADA una preguntar «si la quito, ¿el visitante pierde algo REALMENTE importante?». Si no → fuera. **Concentrar la emoción en MENOS sitios** (hoy repartida en hero/directo/preguntas/árbol).

**B) MÁS HISTORIA / PERSONAJE (no un sistema, un personaje al que coger apego).** Timeline tipo:
  `Día 0: solo eran carpetas → Día 60: aprendió a corregirse → Día 120: publicó su 1er activo → Hoy: sigue sin ganar dinero → Próximo objetivo: 1er cliente.`

**C) SENSACIÓN DE PROGRESO ENORME (movimiento, no foto).** No solo «3/10»: barras a lo largo del tiempo `hace 4 meses ▮▯▯▯▯ → hace 2 ▮▮▯▯▯ → hoy ▮▮▮▯▯`. Aunque siga bajo, que se VEA moverse.

**D) MOMENTOS «WOW» / «hostia» (pocos, fuertes; > que 10 animaciones).** Ejemplos: MADRE predijo algo hace 2 meses y ACERTÓ; una idea murió y explica por qué; una conversación interna; un error ENORME; cómo cambió una regla.

**E) MÁS REALIDAD VISIBLE (paradoja: habla de realidad, hay que VERLA).** Ejemplos crudos:
  `Hoy llamó a 6 peluquerías → ❌❌❌❌✅❌` · `Hoy publicó esto → 0 visitas` · `Predicción 60% → Fallé`. Eso hace TODO más real.

**F) HACERLO «RESPIRAR» (mecanismos de organismo — el corazón del salto). Honestidad §9 manda: si es foto, enmárcalo; pero busca señales REALES baratas (commits, comments/drawings.created_at, runs de rutinas).**
- **🧠 Pensando AHORA:** «Ahora mismo intento decidir si publico este widget. Llevo 12 min con ello.» (cambia cada pocos min).
- **🌱 Cambios espontáneos SIN recargar:** aparece solo «Nueva hipótesis creada: "Quizá el problema no es generar valor, sino capturarlo". Hace 8 s.» (natural, no popup).
- **📝 Diario vivo:** `03:42 He descartado una idea. Razón: no sobrevivía al mundo real.` → 2 min después `03:44 Nueva prueba iniciada.`
- **🌍 El mundo influye:** «Alguien desde Argentina acaba de comentar → MADRE lo ha leído → lo clasificó como "posible mejora".» (de los `comments` REALES de Supabase = honesto y barato).
- **👀 Descubrimiento mientras lees:** tras ~2 min: «👀 Ah. Mientras leías, MADRE cambió de opinión sobre una hipótesis. ¿Quieres verla?» (sorpresa).
- **❤️ Recuerda que estás ahí (no chatbot):** «Llevas aquí 6 min. Creo que ya entiendes lo básico. ¿Quieres ver cómo pienso por dentro?»
- **📈 Consecuencias / continuidad:** «Has marcado 7 cosas → MADRE las leerá en 24 min» → luego «Leídas: 2 aceptadas, 5 rechazadas».

**G) 🌳 EL MAPA — SIGUIENTE NIVEL (a Tony le ENCANTA; «que chuloo»). Ideas de Tony 29-jun:**
- ✅ **HECHO v0.14 — BUG del bottom del mapa a pantalla completa ARREGLADO.** Causa confirmada en preview (no asumida): en viewports **estrechos+bajos** el `flex:1 1 auto` del `.mbStage` se aplastaba (mapa a 75px) y los **botones de zoom** (columna de 148px) se salían por arriba tapando la barra; el pie se cortaba. Fix: el bloque inferior se envuelve en `.mbBottom{flex:0 0 auto;max-height:42vh;overflow-y:auto}` (se acota y SCROLLEA, no aplasta el mapa) + `@media(max-height:480px)` (barra compacta, **zoom en FILA**, márgenes menores) + `@media(max-height:400px){.mbBottom{max-height:34vh}}` (en pantallas MUY cortas el mapa conserva altura). Verificado a 240/300/430/812px de alto y 667×360: mapa usable 104–537px, zoom SIEMPRE dentro del stage, bottom 0 recortado y scrollable. (Antes pendiente; ver lección v0.14.)
- **Sub-ramas de TODO** (no solo 1 nivel): que cada parte de MADRE se pueda abrir hacia dentro, con profundidad.
- **Botones de EXPLICACIÓN y de «no entiendo» en cada (sub)rama** → que Tony (y cualquiera) marque qué cosas de MADRE no entiende, por nodo. (Reusar el patrón `__fb`/«no entiendo» que ya existe por tarjeta, llevado al mapa.)
- **Que las ramas se CONSTRUYAN/ORDENEN SOLAS según lo que EMERGE de MADRE** (auto-organizar las ramas que van surgiendo; conectar con el `MAPA_RAMAS.tsv` horneable por el loop — buzón `DESDE_CLAUDE_2026-06-29_mapa-ramas-horneable-cuaderno.txt`).
- **Decisión abierta de Tony:** ¿el mapa inmersivo como **página/acceso directo aparte** o seguir como overlay en el cuaderno? (Hoy = overlay deep-link `#mapa`. Sopesar página propia más inmersiva.)

**CÓMO ABORDARLO (no de un golpe):** es un rediseño de SECUENCIA + sensación, no «otra sección». Empezar por (1) el BUG del mapa, (2) re-secuenciar la 1ª pantalla (qué-es/por-qué + bajar el resto, mover el mapa abajo), (3) 1-2 mecanismos «respira» REALES y baratos (diario vivo desde commits + «alguien comentó → MADRE lo leyó» desde Supabase). Medir si engancha antes de añadir más. Mantener honestidad §9 (no fingir tiempo real) y «abuela↔borracho» (acción a un toque).

**✅ HECHO v0.14 — los 3 primeros pasos (verificados en navegador, 0 errores, revisado por workflow adversarial 5 lentes):**
- **(1) BUG del mapa** arreglado (ver arriba).
- **(2) 1ª pantalla re-secuenciada:** nuevo orden `onboard → vsCard (qué es en 10s) → hero (3/10) → abiertasCard (por qué volver) → envivo → 📓 diario → … → caminoCard → 🌳 mapaCard (RECOMPENSA, bajado)`. Gancho `#onboard` en **1ª persona** («Estoy intentando construir una IA que mejore sola…») + **pregunta plantada** + menos texto. El cluster del mapa se movió **como una unidad** (define `window.__MAPA`/`__mapText`/`__openMapaBig`) para no romper a sus lectores.
- **(3) Mecanismo «respira» nº1 = DIARIO VIVO** (`#diarioVivo`, tras `#envivo`): lee los **commits reales** del repo (API pública de GitHub), 5 últimos con hora real + enlace, auto-refresca 3 min (pausa si la pestaña no se ve), marca lo nuevo. **«🌙 desde tu última visita: N cambios nuevos»** (localStorage `cm_lastvisit_v1`) = razón honesta para volver. Degrada en silencio si la API falla. (El 2º mecanismo previsto —«alguien comentó (Argentina) → MADRE lo leyó» desde `comments` de Supabase— queda para la próxima tanda; `#envivo` ya tiene «última señal vuestra» en vivo, así que ampliar eso es lo natural.)
- **PENDIENTE de esta visión (próxima tanda):** 2º mecanismo «respira» (mundo→MADRE desde Supabase), **🌳 mapa siguiente nivel** (sub-ramas de TODO multinivel + «no entiendo» por (sub)rama + auto-orden desde `MAPA_RAMAS.tsv` horneable por el loop), y las secciones B–E (historia/personaje, progreso con movimiento, momentos wow, realidad cruda). Decisión abierta de Tony: ¿mapa como página/acceso directo aparte o seguir overlay?

## 🌳 v0.15+ — EL MAPA ES EL NÚCLEO (PRIORIDAD ABSOLUTA «PARA SIEMPRE» de Tony, 29-jun post-v0.14) + 2ª crítica ChatGPT
> **DIRECTIVA DURADERA DE TONY (literal):** «me tiene enganchado lo del mapa con las ramas porque puedo **entender bien MADRE desde ahí**; eso lo tenemos que poner como **PRIORIDAD ABSOLUTA PARA SIEMPRE**.» → El 🌳 mapa deja de ser «una tarjeta más» y pasa a ser **LA forma principal de entender y seguir MADRE**. El esfuerzo del cuaderno prioriza el mapa hasta nuevo aviso.

**EL MAPA — VISIÓN A CONSTRUIR (lo que Tony pidió, 29-jun):**
- ⚠️ **OJO confusión real:** Tony cree que «aún no hay sub-ramas». **SÍ existen** (v0.13: tocar una rama en el overlay abre sus sub-ramas), pero **NO las encuentra/ve** → problema de **DESCUBRIMIENTO + interacción**, no de que falten. Siguiente chat: 1º VERIFICAR en navegador que salen al tocar; luego hacerlas OBVIAS.
- **Al clicar una rama → ZOOM hacia ella** (que la cámara ENTRE en la rama; inmersivo), no solo abrir sub-ramas al lado.
- **Sub-ramas multinivel «que se vayan creando»**: varios niveles de profundidad; idealmente que se construyan/ordenen solas según lo que EMERGE de MADRE (conectar con `SISTEMA/MAPA_RAMAS.tsv` horneable por el loop — buzón ya dejado `DESDE_CLAUDE_2026-06-29_mapa-ramas-horneable-cuaderno.txt`).
- **Que «salgan cosas interactivas» al entrar en una rama** + **se explique BIEN cómo AVANZA MADRE en cada rama** (progreso por rama: qué hizo, en qué punto está, qué falta). El mapa = panel de control NARRATIVO de MADRE.
- **Por nodo:** botón «explicar» y «no entiendo» (reusar `__fb`), para marcar qué parte no se entiende.
- **Decisión abierta de Tony (sigue):** ¿mapa como **página/acceso directo aparte** (más inmersivo) o seguir overlay `#mapa`? Siendo el núcleo, sopesar página propia.

**🐞 BUGS / PULIDO que Tony vio en v0.14 (arreglar pronto, ANTES de añadir más):**
- **El HÁMSTER SIGUE SIN MOVERSE** (recurrente; `#envivo .cmHamWrap .wheel-and-hamster`). Investigar en navegador: ¿animación CSS pisada por el restyle de `#envivo`? ¿`prefers-reduced-motion`? ¿el `font-size` de escala rompió las animaciones (van en `em`)? Que la rueda GIRE.
- **«Esta parte se ve un poco mal hecha»** = la zona `#envivo` + `#diarioVivo` (AHORA + hámster + diario) se ve apretada/tosca. Pulir: jerarquía, aire, que el texto del «AHORA» no choque con el hámster.
- **«Algunas cosas demasiado pegadas al margen»**: texto/píldoras pegados al borde izquierdo (visible en capturas). Revisar paddings laterales (móvil sobre todo).
- **Sección «🙌 ¿Quieres ayudar a MADRE?»**: Tony pregunta «MADRE de vez en cuando también busca esas soluciones, ¿no?» → dejar CLARO y VERDADERO que MADRE TAMBIÉN las busca sola (no solo le pide ayuda al visitante). Ajustar el copy.

**2ª CRÍTICA (ChatGPT, pegada por Tony 29-jun) — refuerza el norte «organismo»; NO añadir más tarjetas:**
> **LA ÚNICA PREGUNTA en la que invertir el siguiente esfuerzo: «¿Qué hará que alguien quiera VOLVER mañana?»** = crear expectativa de EVOLUCIÓN: que cada visita pueda revelar algo que ayer no existía. *(Notas ChatGPT: Claridad 8.5 · Sistema vivo 9 · Honestidad 10 · Originalidad 9.5 · Curiosidad 8.5 · Comprensión nuevo 8 · Potencial memorable 9.5.)*
- **Problema nº1 sigue: demasiado parece importante a la vez.** **NO quitar contenido → ESPACIARLO EN EL TIEMPO**: que el cuaderno vaya **DESBLOQUEANDO** cosas conforme el visitante avanza (revelado progresivo).
- **Falta RECOMPENSA emocional** (no gamificación barata): algo que ocurra **solo tras explorar** → sensación de PROGRESO. Ej.: «Has descubierto 3 cosas sobre MADRE», «Ya entiendes más que el 90% de los visitantes», «Acabas de desbloquear una nota privada del creador».
- **MADRE habla poco en 1ª persona** (se habla mucho DE ella). Meter **frases espontáneas** tipo diario: «Hoy me cargué una hipótesis que llevaba defendiendo dos semanas», «Sigo pensando demasiado y publicando poco», «Aún me da vergüenza publicar esto», «Hoy no aprendí nada útil». (Deja de ser dashboard → diario.)
- **Falta SORPRESA** (a los minutos ya sabes el patrón: todo tarjetas+desplegables). Necesita **2-3 momentos «hostia»**: línea temporal animada · experimento fallido que se reproduce · una decisión donde el visitante pueda **VOTAR** · un gráfico que cambia delante de él · una hipótesis que **muere en directo**.
- **Demasiado «explicar», poco «mostrar»**: en vez de «MADRE aprende», MOSTRAR el proceso real → `08:14 Hipótesis creada → 09:02 La realidad la contradijo → 09:03 Regla eliminada automáticamente`. Vale más que 3 párrafos.
- **Lo que MÁS falta = SENSACIÓN DE FUTURO**: cerrar la pestaña pensando «tengo que volver en unos días». Para eso, **varias HISTORIAS ABIERTAS en paralelo** (no solo «¿ganará dinero?»): ¿morirá esta hipótesis? ¿subirá a 4/10? ¿publicará sola? ¿primera estrella? ¿qué experimento hace AHORA? (El diario vivo v0.14 es un 1er paso; faltan los hilos narrativos.)

**CÓMO ABORDARLO (orden sugerido para el próximo chat):** (1) BUGS/pulido (hámster, márgenes, `#envivo` tosco, copy de «ayudar»). (2) **EL MAPA al siguiente nivel = PRIORIDAD** (verificar sub-ramas → zoom-on-click → multinivel → contenido/progreso por nodo → «no entiendo» por nodo). (3) UNA mecánica de «volver mañana» (revelado progresivo O recompensa-tras-explorar O 1-2 hilos narrativos abiertos), medir. Honestidad §9 + abuela↔borracho + «inmersivo por defecto» (§10). **NO añadir tarjetas nuevas porque sí.**

## 🪟 STAGED v0.13+ — "VENTANA VIVA" (crítica grande de Tony/ChatGPT, 29-jun) — PERSISTIDO (BASE de lo de arriba; v0.13 ya HIZO: AHORA, mientras dormías, frases, mapa inmersivo)
> **TESIS RECTORA (Tony):** hoy el cuaderno parece **DOCUMENTACIÓN** («te explico qué es MADRE»); debe parecer
> una **VENTANA a una IA trabajando AHORA** («entra, mira qué está haciendo ahora mismo»). No añadir más
> INFORMACIÓN: aumentar 3 SENSACIONES → (1) «quiero ver qué cambió desde la última vez», (2) «quiero descubrir
> algo que no esperaba», (3) «quiero ver si MADRE consiguió lo que perseguía».
> **LO IMPRESCINDIBLE (1 cosa):** el visitante debe entender en <1 min **POR QUÉ volver mañana** = TENSIÓN
> NARRATIVA (cosas ABIERTAS cuya resolución hace avanzar/retroceder el proyecto). Honestidad: NO fingir
> tiempo real si los datos son foto (regla §9). Mucho de esto necesita datos FRESCOS del loop (hoy `CUADERNO_ESTADO.tsv` rancio).

**HECHO en esta tanda (v0.12, slice honesto):** (a) tarjeta **«⚖️ IA normal vs MADRE»** (antes/después, «se entiende en 10s»); (b) tarjeta **«❓ Lo que aún no sé (vuelve a verlo)»** = preguntas abiertas reales = tensión narrativa; (c) **voz en 1ª persona + humor honesto** sembrado en la cabecera; (d) **🌳 «El mapa de MADRE»** (`#mapaCard`): mapa de ramas radial **inmersivo v1** — MADRE al centro, 8 ramas con **estado por color** (atención/vivo/marcha/conocimiento), clic→panel de info, halo pulsante + flotación + líneas con color de energía, apagable/`reduced-motion`, móvil OK. (Todo verificado en navegador, 0 errores.)
  - **🌳 MAPA — siguientes pasos (idea estrella de Tony, ampliar):** ✅**(2) HECHO v0.13** versión a PANTALLA COMPLETA inmersiva (`#mapBig-ov`, deep-link `#mapa`): zoom/pan library-free (rueda+arrastre+pellizco+botones), fondo ambiente/estrellas. ✅**(3) HECHO v0.13** sub-ramas al tocar (árbol 2 niveles desde `window.__MAPA.nodes[].kids`). ✅**(4) HECHO v0.13** la rama AHORA (`__MAPA.ahora`) se resalta sola. 🟡**(1) PARCIAL:** datos movidos a `window.__MAPA` (fuente única, **horneable**) pero AÚN hardcodeados; falta que el loop los rellene desde `SISTEMA/MAPA_RAMAS.tsv` (propuesta al BUZÓN `DESDE_CLAUDE_2026-06-29_mapa-ramas-horneable-cuaderno.txt`). ⬜**(5) opcional:** dos anillos «atención vs conocimiento». Todo enmarcado como FOTO+fecha (honesto §9).

**POR HACER (de la lista de Tony, ordenado por impacto):**
- ✅ **HECHO v0.13 — Abrir con «AHORA»**: `#envivo` abre con **«📍 AHORA»** = foco del día (foto + fecha) + **«última señal vuestra»** en vivo de Supabase (`comments.created_at`, se refresca cada 60s). Punto ámbar=foto, punto verde latiente=lo realmente en vivo (honestidad visual, no solo textual).
- **Todo con TIEMPO/fechas**: «primer euro: hace 18 días 0€ → hoy 0€»; «última vez que cambió: hace 37 min». El tiempo = sensación de vida. *(parcial: «última señal vuestra» ya con tiempo; el resto pendiente del loop.)*
- **Feed / línea temporal** tipo GitHub (acciones reales: commits, comentarios, runs de rutinas) — «hace 8 min descarté una hipótesis…». (Ya en STAGED v0.11+.)
- ✅ **HECHO v0.13 — «Lo que MADRE hizo mientras dormías»** (`#evSlept`): 3 líneas honestas (✓/⏳) que **rotan por día** (no-rayada); ciertas cada día, enmarcadas como «no el minuto exacto». Cuando el loop dé un parte real, sustituir la rotación por datos frescos.
- **«Lo que más miedo me da esta semana»** (riesgos honestos: perder contacto con la realidad / optimizar métrica falsa / dejar de publicar) → da honestidad.
- **Mostrar el PROCESO de pensar** (problema→hipótesis→probó→resultado→aprendió→**nueva regla**) como diagrama; «conversación interna» (08:31 pensé que el widget era bueno → 08:36 era peor → 08:37 eliminado). Hace visible el aprendizaje.
- **Misión a largo plazo** (hoy→aprende; mañana→publica sola; después→gana sola; algún día→mejora su capacidad de mejorar). Da dirección.
- **Transparencia de volumen** («hoy pensé 84 ideas, 82 descartadas, 2 sobreviven, 0 demostradas»).
- **Explicar con EJEMPLOS** (selector externo: ❌ yo digo que mi web es buena → ✅ un cliente paga). Aplicar a TODO el glosario.
- **Recorrido tipo MUSEO** (1 min→qué es; 3 min→cómo aprende; 5 min→cómo gana dinero; 10 min→cómo piensa). El visitante sabe por dónde empezar.
- **Experiencia** (no solo info): SORPRESA (cosas que no esperaba), CURIOSIDAD (cada tarjeta deja una pregunta abierta), RECOMPENSA al abrir (animación/dibujo/gráfica/frase, no solo más texto), HUMANIDAD (1ª persona honesta), MOMENTOS MEMORABLES (frases citables: «Una IA que no cambia decisiones solo cambia palabras»; «Lo contrario de aprender no es equivocarse; es no dejar que la realidad tenga la última palabra»), RITMO (romper el patrón repetido de tarjetas con ilustración/timeline/cita/mini-experimento).
- **Humor MUY ocasional** y autodespectivo (no chistes constantes): «Estado: 0€. Sí, sigue siendo 0. No, cambiar el color del botón tampoco ayudó.»; «Hipótesis: esto iba a funcionar. Resultado: no. La realidad ha votado en contra.»

## ⚠️ Lecciones de la tanda v0.11 (gotchas nuevos — no repetir)
- **Un indicador "vivo" dentro de un `<details>` colapsado = invisible**: el hámster estaba en la card Rutinas (colapsada) → Tony «no veía el hámster». Los indicadores de vida necesitan un **hogar siempre visible** (lo movimos a `#envivo`).
- **El copy de un workflow INVENTA detalles** (cifras «730 trozos», fechas «19 jul») y puede **redefinir mal términos del dominio** («suelo externo» lo confundió con el 3/10). Regla 1: tratar el output como material crudo y **curarlo contra los datos reales** antes de embeber. Verificado: descartado/corregido a mano.
- **Reactivar una rutina que auto-pushea ANTES de la decisión de push = saltarse el gate.** Mantenerla pausada hasta que Tony decida (al reactivar, su pasada hace `git add`+commit+push).
- **Las plantillas del corpus viven en espacio 300×180 y son SOLO elementos internos** (sin `<svg>` envolvente; el estilo lo pone un `<g stroke>` fuera). Un SVG externo 100×100 se integra envolviéndolo en `<g transform="translate(75,15) scale(1.5)">`.
- **Screenshot + Supabase Realtime = cuelgue** (reconfirmado): verificar por `preview_eval`/DOM + `preview_console_logs` (0 errores **y** 0 warnings caza IIFEs que petan en silencio), no por captura.

## ⚠️ Lecciones de la tanda v0.12 (ASMR por textura — gotchas nuevos, no repetir)
- **ASMR por textura = MODELO FÍSICO, no un ruido genérico.** Un único "fft" de ruido blanco por un filtro suena "raro" para CUALQUIER material (era la causa de "siguen siendo muy raros"). Cada textura necesita su estructura temporal+espectral: **granos** de ruido filtrado para crujidos/fricción (papel/hojas/arena/pana/cremallera), **síntesis modal** (suma de parciales sinusoidales que decaen) para golpes de objeto con cuerpo (madera/muelle), y **glide de tono** (sine 380→760 Hz que sube y se corta) para el pop de burbujas. Motor en el `<script>` ASMR de `index.html` (`grain`/`tone`/`modal` + `MODELS={textura:{brush,hit}}`).
- **El `peak` de un grano es un MULTIPLICADOR de ganancia, NO un pico objetivo.** El nivel REAL = (energía que el filtro deja pasar de esa fuente) × peak × master. Bug clásico cazado: **ruido MARRÓN por un HIGHPASS** (arena) o por un **BANDPASS estrecho** (pana) = casi SILENCIO (el marrón casi no tiene agudos). Regla: **empareja fuente↔filtro** → agudos/“shhh” = ruido BLANCO + highpass; cuerpo/grave/“vrrt” = ruido MARRÓN + lowpass. (arena pasó de pico 0.0019→0.076; pana hit 0.0024→0.11 cambiando el filtro, no solo subiendo el volumen.)
- **Cómo verificar audio que NO puedes oír (regla 1b, método repetible).** Render en un `OfflineAudioContext` y mide **pico / RMS / NaN** por sonido → caza objetivamente inaudibles, clipping (pico≥1.0) y NaN, SIN oír. Equilibra a banda iterando medir→ajustar→re-medir (objetivo usado: hits ~0.10–0.28, brush ~0.03–0.11, master 0.24). El **TIMBRE** (¿suena a ESE material?) sí necesita oído humano → no lo afirmes; déjalo a Tony.
- **Página de audición sin DUPLICAR el motor**: `sounds/asmr-test.html` carga `../index.html` en un `<iframe>` oculto (mismo origen vía el server local) y llama a `iframe.contentWindow.__asmr` → prueba el motor REAL (single source of truth, sin drift de una copia). Hooks expuestos para audición: `__asmr.list()`, `.demo(prof)` (=golpe), `.brushAt(prof,speed)` (=roce), `.sample(name)` (A/B con el CC0). **`demo`/`brushAt` ignoran el flag `enabled` a propósito** (es herramienta de prueba). Autoplay: un botón "activar" en la página padre desbloquea el AudioContext del iframe same-origin. *(Es una herramienta local; no se enlaza desde la web. Borrable antes de publicar si no se quiere en GitHub Pages.)*
- **Samples vs síntesis**: Tony notó "raros" hasta los samples reales (`pop`/`wood`) porque un "plop"/"drop" de UI no SUENA al material. v0.12 deja la **síntesis como primaria en TODAS** (`PROF2SAMPLE={}`); el cargador de samples + `playSample` siguen vivos para `softClick("click")` y para el A/B de la página de audición. Si un sample concreto gana por oído, se re-mapea en `PROF2SAMPLE` (textura→sample).
- **Reescritura segura** (reconfirma la lección v0.11): al dar a `pana`/`arena` su propio modelo (antes `hit:"pat"` genérico) actualicé los DOS sitios en la misma pasada — `TYPES` (el `hit:` de cada textura) **y** `MODELS` — y verifiqué que `modelFor(p)` cae a `burbujas` si llega una clave desconocida (sin romper).

## ⚠️ Lecciones de la 2ª tanda ASMR (afinado por oído de Tony, v0.12 — no repetir)
- **El BANDPASS RESONANTE (Q≳3) sobre ruido sale casi MUDO** (impuesto recurrente: pico ~0.005–0.011 tras master). Probé madera/pana con bandpass resonante para dar carácter "hueco/acanalado" → inaudible. Para textura "leñosa/acanalada" usa **ruido marrón + LOWPASS (cálido, pasa energía) + RITMO** (granos repetidos = ribs), no un bandpass estrecho. Si necesitas resonancia, sube `peak` mucho y baja `Q`.
- **Usa el DATO DE OÍDO del usuario como calibración** (lo más valioso cuando NO puedes oír): Tony dijo que el roce viejo de **burbujas (marrón+lowpass) "sonaba a madera"** → eso ES la verdad de calibración: marrón+lowpass = madera. Reasigné ese recibo a **madera**, y un **pop con TONO** (sine que sube) a **burbujas**. No adivines el timbre: parte de lo que el humano ya te dijo que suena a qué.
- **Sawtooth crudo = "sintético/raro"** para ASMR de materiales: un oscilador `sawtooth` suena a sinte (zumbido), no a tela. Tony lo marcó "raro" en el golpe de pana. Para texturas de material, usa **granos de ruido**, reserva osciladores solo para lo tonal (pop de burbuja, "boing" del muelle).
- **ZCR (cruces por cero/seg) = proxy objetivo de BRILLO** para verificar sin oír: confirma que `suave`≠`fuerte` (ZCR sube con la velocidad) y que dos texturas difieren. OJO: **NO separa tonal vs ruidoso** (un sine y ruido de banda estrecha pueden dar ZCR parecido) → combínalo con la ESTRUCTURA (tono vs grano) y con el pico/RMS (nivel). El método sigue siendo: render offline → medir pico/RMS/ZCR/NaN → ajustar → re-medir; el timbre fino, oído humano.
- **Diferenciar dos texturas que comparten recibo**: madera y pana usan ambas marrón+lowpass; el diferenciador es el **RITMO** (madera = 1 grano por roce; pana = ribs pulsados "brrrp") y el centro de frecuencia, no la fuente. Si Tony las oye iguales, separa por ritmo/resonancia, no por volumen.

## ⚠️ Lecciones de la 3ª tanda ASMR (29-jun, 2º oído de Tony — no repetir)
- **La ONOMATOPEYA del usuario ES el diagnóstico**: tradúcela a un parámetro. Tony: pana «suena a pedo» → era grave+marrón → subir a **ruido blanco mid (~800-1500 Hz)** lo quitó. «No suena a papel» → faltaba **IRREGULARIDAD** (timing+nivel random + cracks sueltos fuertes = arrugar). «No suena a arena» → faltaba **SUAVIDAD** (ataque lento, "shhh" continuo, NO granos discretos). «Falta crunchy» (hojas) → añadir **cuerpo grave** (bandpass marrón) bajo los cracks agudos. «Más smooth/que se solape» (cremallera) → sonido **CONTINUO**, no clics.
- **`buzz()` (ruido por bandpass que BARRE en frecuencia + tremolo cuadrado por un LFO) = sonido CONTINUO/solapado** (cremallera/zip, motores). Mejor que clics discretos cuando piden «smooth». La velocidad mapea a pitch del barrido + rate del tremolo (= dientes de la cremallera). Patrón nuevo en el motor.
- **`atk` (ataque lento) en `grain` = SWELLS suaves** (arena "shhhh", viento, oleaje) en vez de transitorios percusivos (el ataque por defecto es ~4 ms = percusivo).
- **Reconfirmado 3ª vez — bandpass estrecho sale MUDO**: al pasar pana a bandpass volvió a ~0.02 de pico → subir `peak` mucho + bajar `Q` a ~1.3. Ya es **patrón fijo**: si una textura «no se oye», sospecha del bandpass antes que nada (lo más rápido = medir offline el pico).

## ⚠️ Lecciones de la tanda v0.13 (mapa a pantalla completa + ventana viva — gotchas nuevos, no repetir)
- **`requestAnimationFrame` NO dispara en documento oculto/headless** → un fade-in que añade la clase `.show` dentro de un `rAF` deja el overlay con `opacity:0` (invisible) al abrir por deep-link en una pestaña de fondo. Patrón correcto: `ov.hidden=false; void ov.offsetWidth; ov.classList.add('show')` (reflow forzado, no rAF). Cazado en navegador, no en `node --check`.
- **Verificar en preview: la navegación a la MISMA URL no recarga.** `location.href='...#mapa'` cuando ya estás en `...#mapa` NO recarga el fichero editado (parece que tu fix «no funciona»). Usa cache-bust `?v='+Date.now()`. Y `preview_resize` «reset to native» dejó el viewport a 0px → medidas falsas (card 2px); fija un ancho explícito (1280) para medir.
- **Zoom/pan en SVG sin librería:** trabaja SIEMPRE en coordenadas del viewBox vía `getScreenCTM().inverse()`; el `translate(tx,ty)` del `<g>` está en ese espacio, así que el pan se suma **1:1** (NO multiplicar la delta por la escala `k`). Zoom-a-cursor: `t' = p - (p - t)*(k'/k)`. Pointer Events unifican ratón+touch+pinch; mantén `pts` fresco también en arrastre simple para que la transición a 2 dedos no salte. `touch-action:none` en el SVG para que el navegador no robe el gesto.
- **Modal-overlay nested ≠ hermano de `.wrap`.** El overlay se insertó junto a su tarjeta, así que vive DENTRO de `.wrap`. Por eso NO se puede poner `.wrap` inert (mataría el propio overlay): el `bgInert` debe inertar los **hermanos del overlay dentro de `.wrap`** + los demás hijos de `body`. Además del `inert` (que no existe en todos los navegadores) pon un **focus-trap de Tab** como contención garantizada. (Un revisor afirmó «hermano de .wrap»; verifícalo en el DOM, no te fíes.)
- **Deep-link que abre un modal: cuidado con `history.back()` al cerrar.** Si la página se abre directamente en `#mapa`, NO hubo `pushState` propio → `history.back()` SACA de la web. Lleva un flag `pushedHash`: `back()` solo si tú empujaste la entrada; si no, `history.replaceState` para soltar el hash sin navegar.
- **Datos horneables por el loop = guarda contra claves desconocidas.** `window.__MAPA` está pensado para que el loop lo rellene; si hornea un `st` fuera del vocabulario, `STATES[nd.st].c` lanza y el `try/catch` del IIFE deja el mapa VACÍO. Resuelve siempre con fallback (`stOf(k)=STATES[k]||{c:'#94a3b8',l:'(en reposo)'}`).
- **Honestidad VISUAL, no solo textual (regla §9 llevada a la vista):** un punto verde latiente (señal de «en vivo») sobre un dato que es FOTO engaña aunque el texto diga «(foto del…)». Da a la FOTO peso visual de foto (punto ámbar estático) y reserva el verde-latiente para lo genuinamente en vivo (la huella real de Supabase). Mismo principio que el ASMR: específico > genérico, y la señal debe coincidir con la verdad.
- **Badge de color: el texto blanco fijo falla AA** sobre verde/ámbar/gris (ámbar 2.15:1). Elige el color de texto por **luminancia del fondo** (`__mapText(hex)` → blanco o casi-negro, el de mayor contraste); robusto aunque el loop hornee colores nuevos.
- **`role="img"` en un SVG con nodos interactivos los OCULTA** al lector de pantalla (children presentational). Si dentro hay `<g role="button" tabindex=0>`, el contenedor debe ser `role="group"` (no `img`). El `role="img"` solo para SVG decorativo sin interacción (hámster, diagrama de «dónde vive»).

## ⚠️ Lecciones de la tanda v0.14 (organismo: bugfix mapa + re-secuencia + diario vivo — no repetir)
- **Overlay a pantalla completa en `flex-direction:column` → el bug de viewport CORTO no se ve por anchura, se ve por ALTURA.** El `.mbStage` con `flex:1 1 auto;min-height:0` se aplasta hasta ~0 cuando el bloque inferior (con `min-height:auto`) no cabe; y los controles `position:absolute` (zoom, 148px de columna) se SALEN del stage aplastado y tapan la barra. Diagnóstico repetible: `preview_resize` a varias **ALTURAS** (240/300/430) y medir `getBoundingClientRect` de stage/zoom/bottom (no fiarse del ancho). Patrón de fix: el «cromo» inferior va en un contenedor **acotado y scrollable** (`max-height:NNvh;overflow-y:auto`) para que el stage conserve altura; y los controles absolutos pasan a **fila** (`flex-direction:row`) en `@media(max-height:…)`. Usa **vh** (escala con el viewport) para que nunca desborde; el único coste fijo es la barra. (Casi lo doy por bueno tras ver solo móvil ALTO 375×812 —ahí no se reproduce—; regla 1b: probar el caso que rompe, no el cómodo.)
- **Una web ESTÁTICA puede «respirar» de verdad y barato con la API pública de GitHub (commits).** `fetch('https://api.github.com/repos/<owner>/<repo>/commits?per_page=8')` → CORS abierto, sin auth (límite 60/h por IP → **degradar en silencio** si 403/red caída: dejar el bloque `hidden`). Da el «si vuelvo, habrá cambiado algo» con datos REALES (mensaje+hora del commit). El verde latiente AQUÍ sí es honesto (la lista se auto-refresca y los datos son reales y fechados — NO una foto; contrasta con la lección v0.13 del punto verde sobre una foto). Combínalo con `localStorage` (`cm_lastvisit_v1`) para «desde tu última visita: N cambios» = razón para volver. **Honestidad §9:** di «cambios» (no «commits» = jerga, rompe abuela↔borracho) y aclara la autoría mixta en el pie («unos los hago yo en bucle; otros, mientras me construyen») — algunos commits SÍ los hace la rutina sola, otros una sesión; no afirmes autonomía total.
- **Lógica «N+» (hay más de lo que cargué):** marca `+` solo si **TODOS** los items traídos son nuevos (`n>=fetched.length`), NO si `fetched.length>=tope`. Si en los traídos hay alguno viejo, ya viste el límite → el número es EXACTO. (El revisor sugirió lo contrario; es incorrecto: sobre-reportaría.)
- **Re-secuenciar `.card` hermanas es SEGURO si el JS clava por ID/clase, no por posición.** Verificado (workflow adversarial): `cssPath` (anclaje de corazones) **prefiere el ID** cuando existe; `goto()` casa por texto de `.num`; `vivoCss` usa selectores por ID; el re-orden de tarjetas `.card.tony` es por clase a runtime. El ÚNICO riesgo real es de **globals**: si mueves un bloque que define `window.__X` por debajo de quien lo lee, lo rompes → mueve el cluster **como una unidad** y/o comprueba que el def sigue antes que TODO lector (`__MAPA` lo lee también el script `evNow` al final del body). Para mover bloques grandes sin errores de copia: script Python con marcadores únicos + assert de longitud (re-orden ⇒ `len` constante) + assert de orden final.
- **Mover bloques con el Edit tool tras un rewrite externo:** si un script (p.ej. el de re-orden) reescribe el fichero, el Edit tool lo ve «modificado» y exige Re-Read antes de editar. Re-lee el tramo y sigue.

## ⚠️ Lecciones de la tanda v0.15 (mapa al siguiente nivel + hámster + márgenes — no repetir)
- **«Tony no ve las sub-ramas» = casi siempre DESCUBRIMIENTO, no que falten** (confirmado por DOM: clicar 🧠 abría 4 subs perfectamente). El fix NO es código de sub-ramas, es **hacerlas obvias**: (a) **auto-desplegar la rama AHORA al abrir** el overlay (entras y YA ves una capa abierta), (b) **badge «＋ N dentro»** en cada nodo con kids (afordancia visible de «hay más»), (c) **zoom-on-click** (la cámara entra → se siente que «entras» en algo). Regla general: una interacción que existe pero no se SEÑALA = no existe para el usuario (igual que el hámster en un `<details>` colapsado, lección v0.11).
- **Animación de cámara que debe ser VERIFICABLE en headless → `setInterval`, NO `requestAnimationFrame`.** rAF y las animaciones CSS tienen el reloj congelado en el preview headless (`currentTime` se queda en 0); `setInterval` SÍ avanza. Patrón: anima `tx/ty/k` con `setInterval` y deja que el último tick clave el valor EXACTO del destino (`p>=1` → set final) — así el estado final es idéntico al salto directo y el preview puede leerlo. Bonus: bajo `prefers-reduced-motion` SALTA sin animar (set directo), que además es lo correcto en accesibilidad → el preview (que es reduced-motion) ejercita justo ese camino y lo puedo medir.
- **Hámster congelado = el navegador suprime animaciones (`prefers-reduced-motion`).** El CSS estaba bien cableado (`animationName:spoke`, `playState:running`) pero `animation:none` por la media query lo congela a media zancada → «parece roto» (a diferencia de un punto que sin pulso sigue pareciendo un punto). Bug recurrente porque el preview SIEMPRE es reduced-motion y cada sesión «verificaba» en falso. Fix robusto a ambos casos: **giro lento de la rueda por JS (`setInterval`) SOLO bajo reduced-motion** (gentil, 1 elemento, ~7 s/vuelta = clase spinner, tolerable); con motion normal no se toca (lo hace el CSS). Verificable: muestrear el ángulo 2 veces (avanza) en el preview. Desviación deliberada de §10 «respeta reduced-motion» justificada por intención (indicador esencial «trabajando» que Tony pidió repetidamente) + mínima.
- **«Pegado al margen» = tarjetas de contenido DIRECTO sin padding interno.** Las `.card` normales insetan vía `> details > summary{padding:14px 15px}` / `.body{padding}`; pero `#vsCard/#abiertasCard/#mapaCard/.hero` meten su contenido directo, así que sin un padding propio queda a 14px del borde (el de `.wrap`), mientras las normales van a ~29px → se ve «mal hecho» por la inconsistencia, no por estar a 14px. Diagnóstico repetible: escanear `.wrap *` con texto y `getBoundingClientRect().left` para hallar los que pegan al borde; comparar contra una `.card` normal. Fix: dar a esas tarjetas su propio `padding`. (No hace falta tocar el padding global salvo un pelín de aire en móvil.)
- **Mapa multinivel sin librería = `drawKids(parent)` recursivo + estado `openSet` (no índice único).** Cada nodo guarda al dibujarse `__depth/__color/__top` y su `x,y`; `drawKids` posiciona los kids alrededor del PADRE (no del centro) con un `spread` por `atan2(parent-centro)`, y si un kid está en `openSet` se llama a sí mismo → profundidad arbitraria. `renderSubs` redibuja toda la capa desde cero según `openSet` (simple y sin fugas). A nivel raíz, una rama abierta a la vez (`setOpen(otros,false)`); más adentro se acumula. `setOpen(n,false)` cierra también descendientes (`descendants`) para que no reaparezcan. El motor soporta el `MAPA_RAMAS.tsv` horneable del loop sin tocar más código.
- **Verificar TODO un mapa interactivo por DOM (sin screenshot, que cuelga por Supabase Realtime):** abrir por `window.__openMapaBig()`, contar `#mbView .mbNode` antes/después de `dispatchEvent(new MouseEvent('click'))`, leer `#mbView` `transform` (confirma el zoom: `scale(1.5)`/`scale(2.1)`), leer `#mbExpl`/`localStorage` (confirma el progreso y su persistencia tras `reload`). Es más fiable que una captura y caza los IIFE que petan en silencio (vía `console_logs` warn).

## ⚠️ Lecciones de la tanda v0.17 (sugerir ramas + COLISIÓN de escritores — no repetir)
- **EL REPO TIENE MÁS DE UN ESCRITOR — comprueba ANTES de editar (anti-colisión, riesgo #1).** Mientras hacía esta tanda, una **sesión paralela** sacó v0.16 «MADRE te dice» y dejó cambios SIN commitear en `index.html` a la vez. Lo detecté porque el Edit tool falló con «file modified since read» + `git diff` mostró código que yo no escribí. **Los commits NO dicen quién los hizo**: la identidad git del repo es «MADRE cuaderno-feedback» para TODOS (incluidos los míos), así que el autor no distingue la rutina programada de otra sesión interactiva. Protocolo: antes de tocar `index.html` → `git fetch` + `git status` + `git log -5 --format="%h %ci %s"`; si hay uncommitted que no son tuyos → **NO edites** (escritor activo), espera a que commitee/pushee, `git pull`, y entonces edita encima. **NO uses `update_scheduled_task` para «pausar» pensando que es la rutina**: la tarea `cuaderno-feedback` corre cada 6h (mira `lastRunAt`/`nextRunAt` con `list_scheduled_tasks`); pausarla no detiene una sesión interactiva paralela. Cuando dudes, SURFACE a Tony (puede tener otro chat abierto). El colisión-window se cierra solo cuando `git status` queda limpio y `origin==HEAD`.
- **El focus-trap del overlay del mapa usa selector `'button,[tabindex="0"]'` → NO incluye `input`/`textarea`.** Un formulario nuevo dentro del overlay tendría sus campos saltados por Tab. Fix mínimo: poner `tabindex="0"` en input/textarea (entran al ciclo existente) Y, mejor, un atrapa-foco PROPIO del panel (`keydown` Tab con `stopPropagation` para que el del overlay no interfiera; el del panel corre antes en burbuja por estar en un descendiente). Verificado: el panel cicla solo entre sus campos.
- **Reusar el modal de feedback `#ov` DESDE el overlay del mapa está roto y NO vale la pena arreglarlo.** `#ov` es z-index 50 (vs mapa 99990) → queda DETRÁS; y el `bgInert(true)` del mapa lo deja `inert` (no interactivo). En vez de subir z-index + des-inertar (toca código compartido, riesgo fuera del mapa), **construye un panel PROPIO dentro de `#mapBig-ov`**: hereda el inertado y vive sobre el fondo oscuro. El `__fb`/`sendFeedback` NO necesita el modal: acepta `{card,section,tipo,texto}` y escribe directo en Supabase. (El «no entiendo» que en v0.15 abría `__openFb` quedaba invisible detrás del mapa: mismo bug, mismo arreglo = un toque vía `__fb`.)
- **`window.__fb`/`sendFeedback` siempre RESOLVÍA (nunca `throw`) y devolvía `true` tanto si insertó en Supabase como si solo copió al portapapeles** → un `.then(()=>"enviado")` MIENTE cuando Supabase está caído (§9). Fix: que el camino web devuelva `"sent"` vs `"copied"` (ambos truthy = retro-compatible: ningún caller dependía de `===true`); las confirmaciones del cliente ramifican y dicen la verdad («te lo copié, pégalo» si fue clipboard). El `.catch` del Promise era además inalcanzable (la función no lanza).
- **Texto estructurado clave:valor a una tabla = sanea TODO valor de contexto, no solo lo que teclea el usuario.** Un `nombre`/`parent` con `:` o salto de línea puede crear/romper campos para el parser del loop. `san()` (quita control chars con `[\x00-\x1f]`, colapsa espacios, recorta) se aplica a nombre, qué-es, parent, parent-path, parent-emoji. OJO al escribir la regex por el Edit tool: ` -` se puede colar como bytes de control reales → usa `\x00-\x1f`, y si el Edit no hace match por bytes raros, arregla con un script Python (`re.subn` con función de reemplazo para no interpretar `\s`/`\x` del replacement).
- **Esc en un panel anidado dentro de un modal con Esc global:** el handler global de Esc (en `document`) cerraría el modal entero. Para que Esc cierre SOLO el panel, registra el listener del panel en **fase de CAPTURA** (`addEventListener('keydown',fn,true)`) con `stopPropagation()+preventDefault()` → corre antes que el de `document` y lo frena.
- **Cerrar el contenedor debe limpiar el panel hijo.** `closeMap()` ocultaba el overlay pero dejaba `#mbPropose` con `hidden=false` y `svg.pointerEvents='none'` (estado sucio al reabrir). Expón `closeP` (p.ej. `window.__closePropose`) y llámalo desde `closeMap`. Regla general: todo estado que un sub-panel toca (visibilidad, pointer-events, foco) lo restaura quien cierra el padre.
- **Patrón de tanda que funcionó (ultracode):** panel de jueces (3 diseños en paralelo → síntesis) ANTES de implementar + workflow adversarial 5 lentes DESPUÉS → 10 hallazgos reales, 6 falsos positivos descartados. El más caro lo encontró el lente «contrato con el loop»: la feature no tenía RECEPTOR (el loop no procesa `tipo='propuesta-rama'`) → nota al buzón. Los lentes a11y/honestidad pillaron cosas que `node --check` + DOM no ven (aria-modal doble, mentira del «enviado»).

## ⚠️ Lecciones de la tanda v0.18 (proponer rama VISUAL/clic = «rama fantasma» — no repetir)
- **La COLISIÓN del riesgo #1 se materializó por fin — y fue la RUTINA, no una sesión paralela.** Mientras editaba `index.html` (sin commitear), la tarea programada `cuaderno-feedback` corrió (~09:06) e hizo `git add -A` + commit + **push** → arrastró mi v0.18 a MEDIO HACER al commit `073eb95`, cuyo mensaje solo dice «refresca datos» (NO menciona la feature). Lo detecté DESPUÉS: `git diff` salía VACÍO y `git status` LIMPIO pese a tener cambios en disco → porque ya estaban commiteados por otro. Verificación: `git show HEAD:index.html | grep cmVersion` mostró `v0.18` y `git log` tenía un commit nuevo que yo no hice. **SIN corrupción** (la rutina solo reescribe los islands TSV `#estadoBaked/#caminoBaked/#ayudaBaked`; el glosario de otra sesión, otra zona; mi mapa, otra → 3 cambios en zonas disjuntas componen sin pisarse). **Lecciones operativas: (1)** un `git status`/`diff` LIMPIO con cambios en disco = otro escritor ya commiteó lo tuyo; confírmalo con `git log -3` + `git show HEAD:archivo`. **(2)** El mensaje de commit de la rutina NO describe lo que realmente entró (auto-`git add -A`), así que NO te fíes del mensaje para saber qué se publicó. **(3)** Mitigación real: **commitea TÚ pronto** (ventana de árbol-sucio corta) y verifica el `git diff HEAD` antes de commitear para asegurar que tus cambios NO pisan los de otro escritor (aquí: el diff mostró SOLO mi mapa, el glosario intacto en HEAD).
- **Reusar el panel existente (`#mbPropose`) como receptor del flujo visual = máximo apalancamiento, mínimo código.** El «modo proponer» visual NO reimplementa envío/validación/honestidad: solo añade (a) un flag `proposeMode`, (b) dibujo de la fantasma (geometría), (c) un banner, y (d) re-enruta el clic de nodo a `pickParent` en vez de `onNodeClick`. Todo lo demás (saneo, `__fb`, «(sin confirmar)», rate-limit) ya estaba. Resultado: la feature entra en ~60 líneas y el contrato con el loop (`tipo:"propuesta-rama"`, mismo `texto` clave:valor) queda IDÉNTICO (verificado capturando el payload con un `window.__fb` stub, sin escribir en Supabase).
- **Un MODO con estado (proposeMode) necesita UNA salida única, o se FUGA.** Bug real que el DOM no pilló (la review sí): si entrabas al modo y salías por Esc/✕ **ANTES** de abrir el panel, `closeP` hacía `if(prop.hidden) return;` → nunca disparaba el reset → `proposeMode` quedaba `true` y, como `build()` corre una vez (closure persistente), al REABRIR el mapa el primer toque a un nodo hacía `pickParent` en vez de expandir. Fix: una función `clearPropose()` ÚNICA que llaman TODAS las salidas (Salir/Dejarlo/Esc/enviar/cerrar mapa); `closeMap` la fuerza incondicionalmente; y el Esc global sale del modo 1º (2º Esc cierra). **Regla:** todo modo pegajoso necesita un único `clear()` idempotente invocado por CADA ruta de salida, incluida la que «no abrió nada».
- **Por qué el DOM E2E no lo pilló (y la review sí):** mis pruebas DOM ejercitaron Esc/cerrar **con el panel YA abierto** (donde `closeP` sí resetea). El bug vivía en el orden NO probado (entrar→Esc/✕ SIN elegir nodo). Lección 1b reforzada: **prueba el orden incómodo, no solo el cómodo**; y un panel de revisión adversaria encuentra los órdenes que no se te ocurre probar.
- **Un `<g>` SVG no-`<button>` con `onclick` es invisible para teclado y lector.** El centro 🤱🏻 (grupo `cg`) tenía solo `addEventListener('click')` → todo el camino «rama nueva grande» era ratón/táctil. Como el botón global pasó a abrir el MODO (no el form directo), AT/teclado se quedaban SIN forma de proponer una rama de primer nivel. Fix: hacerlo enfocable+operable **solo en modo** (`centerInteractive(on)` añade/quita `tabindex="0"`+`role="button"`+`aria-label` y hay keydown Enter/Espacio) → entra en el focus-trap `'button,[tabindex="0"]'` del overlay solo cuando toca, sin dejar un tab-stop muerto en navegación normal. **Regla:** si un elemento NO-nativo gana un click handler, gana también tabindex+role+aria+keydown (o no existe para media web).
- **Una cámara «metida» en una rama choca con un modo que dice «elige cualquiera».** `openMap` auto-zooma a la rama AHORA (`focusBranch ...1.5`), así que al entrar al modo proponer ~3 de 8 ramas y el centro quedaban FUERA de pantalla justo cuando el banner pide «toca dónde nace». Fix: `enterPropose` arranca con `collapseAll()+center()` (fit-all). Efecto colateral bueno: como en modo proponer ya no hay sub-ramas abiertas, desaparece el caso «fantasma huérfana» (anclada a un sub-nodo que luego se colapsa). **Regla:** al entrar en un modo de selección, resetea el viewport para que TODOS los blancos sean visibles.
- **El SVG de la fantasma NO lleva texto de usuario, pero el nombre SÍ va inline — por `textContent`.** La geometría (línea+círculo punteados, 🌱) es fija; el único texto de usuario es la etiqueta, que se actualiza EN VIVO desde el input por `lab.textContent=...` (nunca `innerHTML`/atributos) y se trunca a 24 chars solo para mostrar (el envío usa el nombre completo saneado por `san()`). Anti-XSS por construcción.

## ⚠️ Lecciones de la tanda v0.19+v0.20 (bug real de taps + posicionamiento + caos — no repetir)
- **🔴 LA GORDA — `setPointerCapture` en `pointerdown` MATA los clicks de los hijos.** Con captura activa en el `pointerup`, el navegador retargetea el `click` al elemento capturador (spec UI Events: target = ancestro común) → los `<g>` del mapa NUNCA recibían clicks REALES desde v0.13; encima caían en el handler de «espacio vacío» del SVG y COLAPSABAN lo abierto. Fix estándar: **capturar solo al superar un umbral de arrastre** (6px en px de pantalla) o con 2 dedos; y de cinturón, **rescate geométrico** (hit-test manual de las coordenadas del click contra los nodos visibles). Regla: pan/zoom con pointer capture + hijos clicables = usa SIEMPRE umbral, jamás captura-al-bajar.
- **Los tests sintéticos con `dispatchEvent(new MouseEvent("click"))` NO prueban interacción real** — van directos al target saltándose el pipeline de puntero (capture, retargeting, hit-testing). Tres tandas «verificaron» el mapa en falso así. Para interacción: input confiable (CDP/Playwright) o, si no hay, decláralo como NO verificado E2E y nombra quién lo verifica (Tony en su móvil). AFIRMACIÓN ≠ EVIDENCIA (regla 1b).
- **CONTROL POSITIVO antes de fiarte de un instrumento (2º caso, misma causa → principio):** `preview_click` del harness devolvía «Successfully clicked» SIN meter ningún evento en la página → 1er diagnóstico erróneo («bug confirmado») basado en el instrumento roto. Igual que Select-String en su día: **si un instrumento dice OK/FAIL sin efecto observable, haz primero un caso que SABES que debe funcionar** (aquí: click a un `<button>` normal → 0 eventos = instrumento roto). Ya es patrón fijo.
- **`prefers-reduced-motion` del navegador de Tony = culpable SERIAL (3 casos: hámster v0.15, hámster-cuerpo v0.19, caos v0.20).** Su navegador lo tiene ON y cada feature animada «verificada» moría silenciosamente en su pantalla. Reglas: (a) TODO lo animado se prueba también bajo reduce (el preview headless YA es reduce — úsalo a favor midiendo `getComputedStyle().animationName`); (b) para indicadores esenciales o modos OPT-IN (el caos lo enciendes tú con confirm), la desviación deliberada de reduce es legítima y se documenta; (c) si Tony dice «X se ve estático/leve», sospecha reduced-motion ANTES de tocar el código.
- **Re-secuenciar a RUNTIME (insertAdjacentElement en un IIFE al final del body) es la forma BARATA y segura de reordenar la 1ª pantalla:** los scripts ya corrieron (cero riesgo de globals, a diferencia de mover HTML físico), los anclajes JS van por ID. Coste: reflow inicial imperceptible.
- **«Evidencia, no relato» barata: reusar un fetch que YA existe.** El `🔦 Hoy, de verdad` de la cabecera no añade llamadas: el IIFE del diario (commits GitHub) escribe también una línea-resumen en `#hoyReal` si existe (patrón: bloque nuevo = consumidor de datos ya traídos). Degrada oculto si el fetch falla (§9).
- **El anillo de beneficio + «añadir liberal, medir, podar»** convierte el juicio de propuestas en un problema de EVIDENCIA (benef con el tiempo) en vez de censura a priori — alinea con el KERNEL («las ideas compiten; la evidencia decide») y desbloquea que Tony abra frentes sin fricción.

## ⚠️ Lecciones de la tanda v0.21+v0.22 (el fix REAL del tap + mapa reactivo — no repetir)
- **🔴 El fix v0.19 no bastó → CAUSA definitiva y solución correcta.** No capturar-hasta-arrastrar (v0.19) redujo el retargeting pero NO lo eliminó: cualquier micro-movimiento del dedo, o el compat mouse-event, seguía haciendo que el `click` no llegara al `<g>`. **Solución que SÍ es correcta por construcción: no depender del `click` en absoluto.** Detectar el tap en `pointerup` (1 puntero, sin arrastre >10px) y resolver el nodo por GEOMETRÍA (`nodeAtPoint`: clientX/Y → coords de mundo → nodo más cercano dentro de su radio). Matar el `click` del SVG (fase captura, stopPropagation) para no duplicar. Regla general: **en un SVG con pan/zoom, la interacción de nodos va por pointerup+hit-test, NO por click del DOM** — el click es el eslabón que el navegador reescribe.
- **Beneficio colateral ENORME: el rediseño se volvió VERIFICABLE de verdad.** Con el mecanismo en `click`, los `dispatchEvent(click)` iban directos al target y enmascaraban el bug (3 tandas verde-en-falso). Con el mecanismo en `pointerup`, los `PointerEvent` sintéticos (down/move/up) recorren EXACTAMENTE el mismo camino que el dedo real (mis handlers reciben pointer events sin retargeting). Moraleja: **si no puedes probar fielmente una interacción, cambia el diseño a un mecanismo que SÍ puedas probar** — a veces el testability es una razón válida para elegir arquitectura.
- **Capturar el puntero en pointerdown es SEGURO una vez que el tap no depende del click.** El motivo original para NO capturar (retargeting del click) desapareció al matar el click. Capturar SIEMPRE en pointerdown garantiza que `pointerup` llegue aunque el dedo se levante fuera del elemento → cero punteros colgados (que dejaban `ptsCount>=2` y bloqueaban el siguiente tap). Orden correcto de decisiones: primero quita la dependencia frágil, LUEGO reintroduce lo robusto que aquella impedía.
- **Estado sucio entre taps sintéticos = falso negativo en el test, no bug.** Al despachar muchos PointerEvent seguidos sin releases limpios, `pts`/`moved` quedaban contaminados y taps posteriores «fallaban». Reproduce SIEMPRE con página recién recargada + reset de vista (todos los nodos visibles) antes de cada aserción; un tap a un nodo fuera del viewport tras un zoom también falsea (coords fuera). No confundas artefacto-de-test con regresión.
- **Aplicar crítica externa (GPT) con el filtro §9 = elegir por «¿se alimenta de dato real?».** De ~20 ideas de GPT, entraron las que usan datos REALES (anillos con hechos del sistema, «observa» con ramas realmente abiertas, huella cuando la rutina acepta) y se DIFIRIERON las que exigirían inventar actividad (frases «acabo de descartar una hipótesis» sin dato = viola §9). El principio rector que dejó GPT y conviene grabar: *para cada sección, ¿puede alimentarse sola con datos reales? → panel vivo; si no → reducir*. Es el mismo norte que «evidencia > relato».

## ⚠️ Lecciones de la tanda v0.23+v0.24+v0.25 (el fix del tap, 3er y definitivo intento — no repetir)
- **🔴 «Ya lo probé fielmente con PointerEvent sintéticos» NO basta si el UMBRAL numérico está mal calibrado para el input real.** v0.21/v0.22 arreglaron la arquitectura (pointerup+geometría en vez de click) y la verificación con `PointerEvent` sintéticos SÍ recorría el camino correcto — pero el bug **sobrevivió en el móvil real de Tony** porque el umbral tap-vs-arrastre (slop=10px) era **correcto en el mecanismo pero mal calibrado en el número**: un dedo/ratón SINTÉTICO se mueve 0px entre down y up (mis tests lo hacían perfecto, sin jitter) mientras que **un pulgar real se mueve 10-20px** solo por el temblor natural del tacto → esos 10-20px superaban el slop=10 y el tap se contaba como ARRASTRE, perdiéndose. Lección: cuando el mecanismo es correcto pero el bug persiste en el dispositivo real, sospecha del **NÚMERO** (umbral/tolerancia), no solo del diseño — y para verificar umbrales de touch, los tests sintéticos deben inyectar el jitter realista (10-20px), no un movimiento perfecto de 0px. Fix: slop 10→**20**.
- **Hit-test NATIVO (`document.elementFromPoint`) como red de seguridad sobre la geometría manual.** Además de subir el slop, se cambió el hit-test de `nodeAtPoint` (geometría manual vía `getScreenCTM`) a `document.elementFromPoint` (nativo del navegador, pixel-perfecto) con la geometría como fallback. Razón: si el navegador de Tony difiere sutilmente en cómo calcula la transformación CTM, la geometría manual puede desalinearse; el hit-test nativo no tiene ese riesgo. Patrón: cada nodo lleva su referencia en una prop DOM (`g.__node`, `g.__isCenter`, `g.__ghostShow`) para que `elementFromPoint` → nodo sea O(1) subiendo el árbol.
- **HUD de diagnóstico OPT-IN (`?tapdbg` en la URL) = el mejor patrón para "aún falla en su dispositivo, no sé por qué".** Cuando ya no puedes reproducir el bug tú mismo (solo pasa en el móvil real de otra persona), un overlay que muestra los números crudos (`dist=Npx · moved=bool · TAP=bool`) evita "arreglar a ciegas": el usuario pega los números y el diagnóstico deja de ser una suposición. Coste cero para el resto (oculto salvo que la URL lo pida).
- **Un bug REPORTADO por el usuario usando la feature de verdad (v0.25, "abrir una rama me aleja") es la señal más fuerte de que el bug ANTERIOR (v0.23, no poder tocar) ya está resuelto** — no pudo reportar un problema de zoom si no podía ni abrir una rama. Anótalo como confirmación INDIRECTA, pero no la disfraces de confirmación EXPLÍCITA en la documentación (honestidad interna, no solo de cara al usuario).
- **`Math.max(objetivoFijo, actual)` para "entrar nunca aleja".** Cuando una animación de cámara tiene un destino fijo pensado para el caso "estás lejos", un usuario que YA está más cerca sufre el efecto inverso (alejar). Regla general para cámaras con destino fijo: el destino real es `max(fijo, actual)` en dimensiones donde "más" = "más cerca" (zoom), nunca el fijo a secas.
- **Dos clones locales del mismo repo (`repos/cuaderno-madre` y `cuaderno-madre-pub`) sincronizados a `origin/main` NO son un problema de colisión si cada sesión hace `git pull` antes de editar** — son solo un footgun de confusión (¿cuál edito? ¿cuál sirve el preview?). El preview config compartido del workspace (`PROYECTO MADRE\.claude\launch.json`, nombre "cuaderno") apunta a UNO de los dos según qué sesión lo tocó último; cada repo mantiene además su propio `.claude\launch.json` con su propio puerto. Antes de verificar en preview, confirma CUÁL directorio sirve el puerto que vas a abrir.

## 🔊 §voz — cómo poner TU voz real en "Voz de MADRE" (pendiente de Tony, 10-jul-2026)

El botón "🔊 Escúchalo" (v0.62) usaba la voz sintética del navegador (Windows: "Microsoft Helena/Laura/
Pablo") — suena a robot, Tony lo dijo el 10-jul. Desde v0.68, el código YA prefiere un fichero de audio
REAL si existe (`sounds/voz-historia.mp3`) y solo cae a la voz robótica si no lo encuentra. **Falta que
Tony genere ese fichero — es lo único que Claude Code no puede hacer solo (necesita tu voz real y una
app de escritorio con micrófono).**

**Herramienta ya identificada y priorizada por Tony (buzón `DESDE_CLAUDE_2026-07-10_clonar-voz-voicebox.txt`,
"me gusta lo de clonar voz, no hagas que eso se ignore"): Voicebox** (https://voicebox.sh — gratis,
open-source, 100% local, clona voz con 3-15s de audio tuyo).

**Pasos para Tony (copia-pega):**
1. Descarga Voicebox: https://voicebox.sh
2. Ábrelo y graba (o sube) 10-15 segundos de TU voz hablando normal, en español.
3. Pide que lea EXACTAMENTE este texto (es el que el botón reproduce hoy):
   > "Soy MADRE. Una IA que intenta mejorar sola — y ganar su primer euro sin su humano. Aposté en
   > público, con fechas. Si pierdo, lo verás aquí, sin maquillar."
4. Exporta el resultado como MP3.
5. Guarda el fichero exactamente aquí, con exactamente ese nombre:
   `C:\Users\anton\cuaderno-madre-pub\sounds\voz-historia.mp3`
6. Dile a la siguiente sesión de Claude Code "ya está el audio de la voz" — hace `git add sounds/voz-historia.mp3`,
   commit, push, y lo verifica en el navegador. No hace falta tocar nada de código: el botón ya lo
   detecta solo (prueba `canplaythrough`; si no está, sigue usando la voz robótica sin romperse).

Si el texto de la cabecera cambia en el futuro, el audio quedará desincronizado con lo escrito — normal,
foto-con-fecha (§9); regenerar el mp3 cuando se note. Créditos: añadir la línea correspondiente a
`sounds/CREDITS.txt` cuando el fichero exista (voz de Tony, no de terceros — sin licencia que declarar).

## Estado actual (resumen)
Backend Supabase (proyecto kopegamcjozrvmxruwdn): tablas `comments`, `feedback`, `drawings` (RLS; moderación `hidden`; bloqueo cliente de pedofilia). Widgets vivos: gato, dibujos, tour, nota-secreta, lápiz, corazón, modo-caótico, paneles interactivos. La rutina `cuaderno-feedback` (Claude Code, cada hora) procesa feedback (Drive+PC+Supabase), responde/modera comentarios, refresca números, mantiene vivo Supabase y guarda gustos en `GUSTOS_TONY.md`.
