## v1.71 — 2026-07-15 · Automático primero + aviso humano imposible de perder
- **Autonomía por defecto:** una sesión no puede depender de que Tony recuerde una skill o relea un chat. El carril humano solo admite identidad, banco/dinero, CAPTCHA/2FA, gusto estrictamente personal y acciones físicas inevitables; estas últimas exigen evidencia de haber agotado al menos dos vías entre API/conector, navegador y script/robot.
- **Visible desde la portada:** si existe una excepción pendiente, la franja superior enseña un botón con el recuento y un objetivo táctil mínimo de 44 px. Al pulsarlo revela y enfoca la tarjeta correspondiente en `MADRE te dice`, aunque el recorrido progresivo aún tuviera esa sección plegada.
- **Acuse honesto:** `✓ Lo he visto` solo se retira cuando el canal devuelve `sent`; una copia local, una caída de red o un error conservan el botón. El recibo prueba un toque desde ese navegador en modo creador, no autentica legalmente a Tony y nunca autoriza pagos, borrados ni acciones irreversibles.
- **Fiabilidad y privacidad:** la carga escucha el momento real en que Supabase queda listo, reintenta fallos transitorios y no abandona a los 12 s. La interfaz pública oculta título y detalle, pero el REST es anónimo por diseño: las tarjetas rechazan secretos y declaran este límite sin fingir privacidad criptográfica.
- **Selector:** el primer caso humano real debe terminar en acuse o seguir visible. Si Tony descubre una dependencia únicamente por chat, el mecanismo ha fallado.
- **Revertir:** `git revert <commit de v1.71>`.

## v1.70 — 2026-07-15 · Referido transparente + objetivos de Caos sin carreras
- **Referido sin letra pequeña:** Comentarios fija una respuesta honesta sobre el uso de Claude y enlaza el pase real de Tony. Declara que es referido, el posible crédito para Tony, los pases limitados, la caducidad, el método de pago y la conversión a Pro si no se cancela.
- **Acceso y claridad:** el CTA abre en otra pestaña con `noopener sponsored`, conserva un objetivo táctil de 44 px, contraste medido y presencia tanto en la sección como en el panel lateral.
- **Caos estable:** al acertar un objetivo contextual, anillo y flecha se limpian y el director espera 1,4 s antes de proponer otro. Evita que el temporizador sustituya la recompensa por una flecha nueva en el mismo gesto.
- **Verificación:** oferta, renovación, contraste, panel, tour 7/7 y 375/1280 px; además, cinco ciclos dirigidos de objetivo→toque→reintento terminaron sin relanzamientos ni capas huérfanas.
- **Revertir:** `git revert 8968658`.

## v1.69 — 2026-07-15 · Cuaderno como puerta común a las webs de Tony
- **Navegación:** el final del Cuaderno reúne cuatro destinos públicos comprobados: FanRank, Neuro-Fuerza, Salón Vera y el perfil con todos los proyectos públicos de Tony. Los enlaces abren en otra pestaña para conservar el punto de lectura.
- **Selector real:** el primer envío confirmado a cada destino durante una sesión reutiliza el canal existente de feedback con el tipo `navegacion-proyecto`; los toques repetidos no inflan la señal y un fallo queda libre para reintentar. Si almacenamiento o canal no están disponibles, la navegación sigue funcionando y no se finge una medición.
- **Claridad y acceso:** la cuadrícula se reduce a una columna en móvil, conserva objetivos táctiles mayores de 44 px, foco visible y traducción ES/EN mediante el contrato global `data-en` ya existente.
- **Backend:** sin cambios ni despliegue nuevo; se reutiliza el canal público ya operativo del Cuaderno.
- **Revertir:** `git revert <hash de v1.69>`.

## v1.68 — 2026-07-15 · Primer roce ASMR + objetivos reales en Caos + MyInstants autorizado
- **Roce desde el primer gesto:** una pasada horizontal deliberada activa el ASMR sin exigir invertir el movimiento. El guard sigue rechazando el scroll predominantemente vertical.
- **Laboratorio / feedback:** cada prototipo responde `Coincide: Sí / No` en su propia tarjeta, persiste y permite retirar el veredicto; el diálogo queda solo para matizar. Feedback general incorpora una ✕ superior de 44 px con devolución de foco.
- **Herramientas:** el hub prioriza Caos, Laboratorio y ASMR, registra uso local y reordena lo no activo por frecuencia y recencia sin perder un orden estable inicial.
- **Caos contextual:** activar Caos enciende ASMR. Un director señala controles reales del Cuaderno con anillo, flecha, nombre y recompensa al tocarlos; anillo y flecha siguen al objetivo si todavía se está moviendo, mantiene una sola escena, evita formularios/diálogos y limpia en Escape, Calma, salida, pestaña oculta y `pagehide`.
- **MyInstants:** la interfaz aplica la autorización explícita de Tony para reutilización dentro de MADRE, conserva la URL como procedencia, autocompleta fuente/derechos y mantiene seguridad y deduplicación. Si la URL cambia a otro banco, retira también la confirmación que había autogenerado para evitar una falsa atestación. La Edge Function preparada refleja la misma política, pero continúa sin desplegar.
- **Verificación:** el sensor E2E parte de un contexto fresco para que ningún click previo pueda ocultar la regresión del primer roce y cubre controles directos, ✕, orden adaptativo, Caos→ASMR, spotlight anclado, política MyInstants, 375/1280 px, movimiento reducido y limpieza.
- **Revertir:** `git revert <hash de v1.68>`.

## v1.67 — 2026-07-15 · Combate ASMR 67 + bestiario + archivo multimedia visual
- **Invocación y combate:** el monstruo sigue apareciendo exactamente a 67, pero el roce normal suma `0,34` y cada explosión `0,45`, así que ya no se invoca enseguida. Al aparecer abre una batalla `0/67`: los golpes encadenados hacen combo (base 7; base 11 en Caos), el monstruo esquiva, entra en furia, cae con animación propia y el mundo permanece 20 s. La lluvia material usa 10 emotes en normal y 18 en Caos; la victoria conserva la densidad fullscreen x5 ya existente en Caos.
- **Temática material:** durante el encuentro el color y la textura del ASMR pasan al fondo, tarjetas, portada, barra de estado, cintas y pie del Cuaderno. Calma y `prefers-reduced-motion` conservan información, botones y monstruo estático sin tormenta ni sacudidas.
- **Bestiario + pegado rápido:** `media.html` enseña los 13 mundos ASMR con sus emotes de referencia y un botón por criatura. Elegir uno permite pegar con `Ctrl+V` o arrastrar PNG/JPG/WEBP/GIF, escribir una sola frase y guardar en IndexedDB un borrador privado ya etiquetado para ese monstruo; no publica ni salta seguridad/derechos.
- **Coincidencia y ejemplos:** todas las fichas muestran el ejemplo visual o la muestra sonora que se está juzgando. «Coincide: Sí / No» se responde directamente en la tarjeta y queda persistido; «Añadir matiz» es opcional y conserva el veredicto directo.
- **Cuerdas visuales:** cada ficha de la mesa tiene dos puertos táctiles A/B de 44 px. Tocar dos puertos dibuja la curva, registra piezas, puertos, relación, origen y fecha, y permite reutilizar el mismo puerto para conexiones N:N. El formulario queda plegado como alternativa accesible. En móvil las fichas se compactan sin reducir los puertos; el verificador detectó y eliminó un solape real entre puertos vecinos.
- **Verificación:** `herramientas/verificar_pagina.py` ejecuta a 375/1280 px el ciclo invocación→combate→victoria→fijar→cerrar, mide tema/animaciones/ritmo, prueba ambos veredictos, dos cuerdas desde el mismo puerto, movimiento por teclado, Ctrl+V con Blob+metadatos, ES↔EN, 44 px, overflow, offline y movimiento reducido. Resultado local: `OK`.
- **Backend:** sin cambios ni despliegue; la entrada multimedia privada continúa preparada pero apagada hasta aprobación explícita.
- **Revertir:** `git revert <hash de v1.67>`.

## v1.66 — 2026-07-15 · Caos vivo + monstruos ASMR + mapa lateral + ruta divertida
- **Caos / 6·7:** los eventos duran más y siguen apareciendo con cadencia acotada aunque no haya toques: XD, frases, juguetes explotables, chistes del gato y líneas de velocidad al hacer scroll. Cada 6·7 reproduce una sola mezcla MLG, abre una escena mayor y ofrece activar Caos desde la derecha; Escape, Calma, pestaña oculta y salida limpian escena, temporizadores y también el gato visitante.
- **ASMR:** rozar y explotar suma un contador por material. Cada 67 puntos aparece un monstruo enorme, el Cuaderno adopta ese bioma durante 20 s y se puede fijar o cerrar. En Caos la densidad fullscreen sigue siendo x5. El guiño de descubrimiento conserva su llamada visible 4,8 s, anima 2,35 s y reinicia su único reloj al renovarse para que una llamada vieja no apague la nueva. Las 32 tarjetas completas reproducen su prototipo; sus controles no duplican el sonido. El juicio semántico separa título, emoji, descripción, uso sugerido y nota, bloqueando doble envío.
- **Mapa / comentarios:** en escritorio el panel explicativo ocupa el lateral izquierdo y el mapa conserva toda la altura; nodos y subramas crecen, se separan y nacen escalonados. El universo queda a pico máximo 0,0075 frente a 0,045 del nodo, y fondo/zoom no disparan sonido. En móvil Comentarios abre la tarjeta real; en escritorio usa panel opaco con cabecera/compositor fijos y lista propia, desplazando la página para no cubrirla.
- **Entrada sencilla:** nueva `🎢 Ruta divertida` de 7 paradas reutiliza ASMR, dibujos, logros, Caos, gato, comentarios y FanRank; conserva retorno al origen. Los tecnicismos visibles empiezan con explicación corta y enlazan al glosario real. El mapa muestra primero título/origen/descripción y deja métricas/juicio tras un único desplegable. La ruta, el HUD/ambiente de Caos y los estados ASMR cambian completos entre ES/EN.
- **Verificación:** `herramientas/verificar_pagina.py` mide a 375/1280 px audio 6·7 contra su mezcla directa, Caos autónomo/scroll/Escape y limpieza del gato, tarjeta y feedback ASMR, contador/monstruo/bioma, ES↔EN, ruta, glosario, layout/sonido del mapa y comentarios sin solape. Resultado local: OK en pasadas consecutivas.
- **Revertir:** `git revert <hash de v1.66>`.

## v1.65 — 2026-07-15 · Archivo multimedia + feedback semántico de sonidos
- **Qué cambió:** `media.html` añade galería aprobada, preescucha, filtros, corazones reversibles con clave de cortesía `tetris`, formulario de archivos/URL con metadatos y una mesa visual de relaciones muchos-a-muchos con arrastre, teclado, notas y cuerdas. El Laboratorio ASMR añade «¿Coincide con lo que dice ser?» a sus 32 candidatos, un juez específico para 6·7 y enlaces directos a subir o explorar recursos. El hub derecho incorpora Feedback general y Archivo multimedia; el modal general deriva audio/vídeo al nuevo buzón.
- **Seguridad / backend:** migración y dos Edge Functions preparadas, todavía **NO aplicadas** sin el OK explícito de Tony. Ambos buckets (`media-quarantine` y `media-approved`) son privados; todo activo exige una copia revisada e inmutable y `media-public` revalida el estado en cada lectura, por lo que retirarlo vuelve su URL 404. La entrada añade kill-switch apagado, límite global+por IP antes de leer multipart, MIME mágico, hash, deduplicación, RLS, auditoría y gates separados de seguridad/derechos. Si el endpoint no está activo, el navegador guarda Blob + metadatos en IndexedDB y dice que no se publicó.
- **Verificación:** `herramientas/verificar_pagina.py` incorpora sensores E2E a 375/1280 px para 44 px, overflow, ES↔EN dinámico, un solo audio activo, Tony, feedback de coincidencia, MyInstants multipart, N:N, teclado, caída/IndexedDB y movimiento reducido, además de contratos estáticos del kill-switch y Storage privado. `deno check` valida ambas funciones.
- **Revertir:** `git revert <hash de v1.65>`; la migración no se aplica con este commit.

## v0.33 — 2026-07-10 · Reconstrucción total: "la película en 3 actos"
- Auditoría 12 lentes + panel 3 arquitecturas + juez (sesión Fable, uso ilimitado de Tony).
- Coherencia: purga peluquería (frente real: distribución/Aurova/activos), fix parseEstado (builds de junio congelados), "Hoy (28 jun)"→fechas absolutas, 79→800+ vídeos, cadencias reales, decisiones=archivo.
- Portada 3 frases + CTA héroe al mapa; re-secuencia en 3 actos (ACTO I apuesta / ACTO II construcción / EL GIRO / ACTO III presente).
- El Marcador: palmarés, estados por fecha (⏳/🔔 HOY/⚖️ vencida automática), notario git por apuesta, 🔔 veredictos.ics + RSS.
- ⭐ El Giro: 1ª señal externa (8-jul, verificada por API) con enlaces comprobables.
- Un solo tour (modal 700ms + tour viejo + quickPick, muertos); citas del caos etiquetadas 🃏.
- og-image.png 1200×630 + favicon + twitter:card large + evidencia cruda enlazada.

# CHANGELOG — Cuaderno MADRE

Registro de cambios aplicados al cuaderno por la rutina **cuaderno-feedback** (y a mano).
Cada línea: fecha · qué cambió · por qué (qué feedback) · cómo revertir (`git revert <hash>`).

## 2026-07-07 — v0.32: zoom de verdad al tocar + centro resetea + riesgo actual
- **Qué cambió:** (1) **Zoom (2º reporte de Tony: «sigue sin haber zoom suficiente donde cliques»)** — suelos subidos (raíz 1.5→1.9, sub-rama 2.1→2.6) y AHORA CADA TOQUE ACERCA un 25% más (techo 3.2; nunca aleja — v0.25 intacta). Fórmula verificada: 0.7→1.9→2.38→2.96. (2) **El centro 🤱🏻 resetea la vista** al tocarlo (colapsa ramas + encuadre general; en modo proponer conserva su papel de "rama nueva grande"). Hint actualizado. (3) **«🚨 Lo que más miedo da hoy»** dentro de la apuesta pública: el riesgo real escrito (que el problema no fuera la distribución sino el producto — se sabe el 15-jul). (4) Regla operativa en CONTINUAR: toda tanda visual usa el arsenal propio (ui-ux-pro-max + heurísticas + referencias) antes de improvisar.
- **Verificación:** 0 errores consola; v0.32; fórmula de zoom ejecutada en vivo con 5 casos (incluye no-alejar desde 3.4); riesgo renderiza; hint nuevo. El toque real en móvil lo valida Tony (siempre).
- **Revertir:** `git revert <hash de esta tanda>`.

## 2026-07-07 — v0.31: LA APUESTA PÚBLICA en la cabecera
- **Qué cambió:** bloque `#apEstado` dentro del onboard (sin tarjeta nueva — consolidar): chip ámbar «🟡 APUESTA SIN DEMOSTRAR» + texto que reencuadra toda la web como **apuesta pública con fechas** (10/15/19-jul · 1-ago · 1-sep) y con condición de derrota escrita («si la realidad dice NO, el fallo se enseña aquí y se cambia el plan»). Es el ítem nº1 de la última ronda externa («que la web sea un experimento con condiciones de victoria y derrota, no una promesa abierta»), en versión honesta: solo fechas y consecuencias que YA existían escritas en el sistema. La propuesta del externo de un «veredicto final 1-ene-2027» se RECORTÓ al siguiente juez real (regla nueva de Tony: horizonte ≤3 días, tope 2,5 semanas; los jueces fechados son citas, no planes).
- **Verificación:** 0 errores consola; v0.31; bloque presente con chip visible.
- **Revertir:** `git revert <hash de esta tanda>`.

## 2026-07-07 — v0.30: los 3 reportes de Tony (mapa/ASMR/caos) + motivo para volver
- **Qué cambió:** (1) **Mapa (foto de Tony: «clico un nodo y no hace zoom; el texto de abajo ocupa mucho»)** — causa doble: el panel inferior podía comerse 42vh y ROBABA altura al mapa (parecía des-zoom), y re-tocar un nodo ya abierto no movía la cámara (solo zoomeaba al ABRIR). Fix: `.mbBottom` acotado a 30vh (26vh en pantallas bajas; el detalle scrollea) + TODO toque re-centra el nodo con zoom mínimo 1.5/2.1 (v0.25 sigue: nunca aleja). (2) **ASMR** — «no viene encendido»: un apagado antiguo quedaba pegado en localStorage para siempre → bump `cm_asmr_v2→v3` (re-ON por defecto, respeta apagados nuevos); las burbujas eran INAUDIBLES al rozar (pico 0.05) → suelo 0.16 + chispa desde velocidad media; **2 texturas nuevas**: cristal (tink de copa + campanilla modal) y slime (squish húmedo + plof gordo) — timbre a validar por el oído de Tony en `sounds/asmr-test.html`. (3) **MÁS caos**: 2 travesuras nuevas CLICABLES (bolas que ruedan por el suelo y globos que suben; explotan al tocarlos y suenan gratis por ser `<button>`), ráfagas más frecuentes (0.11→0.16) y más gordas (6-11). (4) **Motivo para volver** (revisión externa, versión honesta): chip `#chipVeredicto` en el hero con cuenta atrás al PRÓXIMO veredicto real (10/15/19-jul·1-ago·1-sep, mismas fechas de la tarjeta 🔮; clic → la abre) + `#visitLine` («primera visita» / «visita nº N, volviste tras D días») por localStorage, cero servidor. (5) **🌐 English fijo arriba a la derecha** (Tony: «debería ser de lo primero»); se oculta con overlays abiertos.
- **Verificación:** 0 errores/0 warnings; v0.30; langEn `position:fixed top:10px right:12px` (computado); chip veredicto «en 3 días» correcto (7→10 jul); visitLine pinta; ASMR `isOn()=true` con clave v3; cristal y slime salen en la rotación de texturas de la página; mapa: `.mbBottom` 243px (antes ~340) y auto-zoom a `scale(1.500)`. El RE-TOQUE con dedo real lo valida Tony en su móvil (lección v0.19: el input sintético no vale para el tap del mapa).
- **Revertir:** `git revert <hash de esta tanda>`.

## 2026-07-06 — v0.29: comentarios bonitos + 👍 que SÍ se marca + 🌐 English + muro de misiones
- **Qué cambió (los 3 reportes de Tony + 1 de la revisión externa):** (1) **El 👍 de comentarios "no salía marcado"** — causa: el estado `.on` era solo un cambio de color de texto en un botón sin fondo (invisible); ahora los botones de comentario son CHIPS con borde y el votado se rellena con gradiente acento + texto blanco + animación pop (verificado por estilos computados; la 1ª medición engañó por leer a mitad de la transición de 150 ms — control positivo con `transition:none`). (2) **Sección de comentarios restilada**: avatar con inicial y color propio por nombre (hash→hue; MADRE conserva su 🤱🏻), tarjetas con hover-lift, selector de archivo nativo feo → botón pastilla (`::file-selector-button`), contador como píldora. (3) **🌐 English**: enlace en la cabecera vía Google Translate (la página declara `lang="es"`, así el navegador también lo ofrece solo) — el 80/20 honesto frente a traducir a mano 500 KB. (4) **"¿Quieres ayudar?" → muro de MISIONES**: frase grande («No necesito horas: UN enlace puede cambiar más que cien comentarios»), chip 🎯 misión por ítem y CTA al pie de cada uno. El CONTENIDO de los ítems sigue siendo del island `#ayudaBaked` (escritor = la rutina/loop; la mejora de los textos va por el buzón, no se pisa aquí).
- **Por qué:** Tony (6-jul noche, con captura): «el botón de comentarios está bugeado, la sección se ve muy fea, ¿y el idioma?» + revisión externa de la sección ayudar (misiones concretas de 30 s).
- **Verificación:** 0 errores/0 warnings; v0.29; chip marcado = blanco sobre gradiente (computado); avatar con inicial y tono por nombre; `.misTop` + 5 misiones pintadas; enlace EN presente.
- **Revertir:** `git revert <hash de esta tanda>`.

## 2026-07-06 — v0.28: auditoría de conversión aplicada (recorrido guiado 90 s + frase-clic + «existe para…» por rama + promesas rancias fuera)
- **Qué cambió:** (1) **Recorrido guiado de 90 s** (`#tourBtn` en el onboard → overlay `#tourOv` de 6 pasos): MADRE lleva al visitante de la mano por 6 secciones REALES (vs → nota → predicciones → mapa → señales → cierre), con glow en cada parada, Esc para salir, targets resueltos al pulsar (lazy), `prefers-reduced-motion` respetado, todo `textContent`. Era la mejora nº1 de la revisión externa («la mayoría abandona si tiene que construir el modelo mental sola»). (2) **Frase-clic** en el onboard: «cada vez que me equivoco, convierto el error en una regla — para que no pueda repetirse». (3) **Campo `para` («Existe para…», 1 frase) en las 8 ramas** + render en el panel grande (`.mbPara`) y en la tarjeta pequeña (`.mi-para`) — «no son carpetas, son órganos». (4) **Credibilidad:** fuera la promesa falsa «se refresca cada ~30 min» (el motor migró el 4-jul a varias pasadas/día); fallback del «última foto» ya no dice «21-jun (congelado)»; fecha del pie de página 3-jul → 6-jul.
- **Por qué:** misión de Tony (6-jul, endosando la revisión externa): «auditoría despiadada de conversión: entra como un desconocido, señala todo lo que haga abandonar y arréglalo directamente».
- **Verificación:** 0 errores/0 warnings; tour recorre los 6 pasos con glow correcto y cierra limpio (botón mapa solo en el paso final; Esc cierra; sin glow huérfano); 8/8 ramas con `para` pintado en ambos paneles; móvil 375px sin overflow y caja del tour dentro del viewport; v0.28 en el pie.
- **Revertir:** `git revert <hash de esta tanda>`.

## 2026-07-06 — v0.27: el mapa apuesta por escrito (juicio por rama) + tarjeta «Se juzga solo» + foto del mapa al día
- **Qué cambió:** (1) **`window.__MAPA` refrescado a la foto REAL del 6-jul** (la anterior era del 3-jul y ya mentía): la rama AHORA pasa de 📓 a 🛡️ (semana de EXPOSICIÓN: usage-guard y session-triage ENVIADOS al catálogo oficial, en revisión), el loop ya no «se despierta ~48 veces al día» (migró el 4-jul a 4 pasadas/día), Activos publicados sube benef 4→8 con sus porqués reales, Memoria estrena la sub-rama «Registro de capacidades», session-triage aparece como kid propio. (2) **Campo nuevo `juicio` por rama** (`{cree, cambia, fecha}`): qué cree MADRE de esa rama, qué le haría cambiar de opinión y cuándo la juzga la realidad — el panel del mapa grande lo pinta (`.mbJuicio`, todo `textContent` anti-XSS, horneable por el loop). Las 8 ramas lo llevan; solo fechas/datos reales del sistema (15-jul kill-criterion, 1-ago recall, 1-sep registro, confianza ~40% de la hipótesis IA-barata). (3) **Tarjeta nueva `#prediccionesCard` «🔮 Se juzga solo»** tras «Señales del mundo»: 8 predicciones REALES con fecha y estado — 3 falladas (tap del mapa 29-jun, filtro barato 1-jul, regla podada 4-jul), 1 acertada (triaje 0% pérdidas 4-jul) y 4 en juego (10-jul veredicto catálogo, 15-jul cuello=distribución, 1-ago chuletas, 1-sep registro). Los fallos se enseñan a propósito: son la prueba de que juzga el calendario, no MADRE.
- **Por qué:** Tony (6-jul): «mejora la web, sobre todo el mapa de ramas» + revisión externa del cuaderno (GPT): su ítem nº1 = «historial de predicciones / ¿qué me haría cambiar de opinión?» — aplicado en versión honesta (cero datos inventados; solo falsadores que ya existían escritos en el sistema).
- **Verificación:** página carga con 0 errores/0 warnings de consola; `__MAPA` con 8/8 juicios; el panel del mapa pinta ⚖️+🔮+📅 en la rama AHORA (auto-abierta) con estilos correctos (inspect); tarjeta con 8 filas y chips ✅/❌/⏳ con sus colores; móvil 375px sin overflow horizontal y fechas dentro de su fila; 8 nodos en la tarjeta pequeña. Limitación del instrumento: `preview_screenshot` se cuelga con esta página animada (la página responde a eval/inspect al instante; la 1ª captura de la sesión sí salió) — la verificación es DOM+estilos.
- **Revertir:** `git revert <hash de esta tanda>`.

## 2026-07-06 — UX móvil: hint imagen + mensajes sin-conexión más claros
- **Qué cambió:** (1) Label e hint del campo de imagen del formulario de feedback: antes decía «pégala con Ctrl/Cmd+V» (solo funciona en PC); ahora clarifica que en móvil hay que usar el botón de subir. (2) Cuatro mensajes «pégalo en tu BUZÓN» (fallback sin-conexión en el formulario general, el comentario de rama del mapa y la propuesta de rama) reemplazados por «Sin conexión — reinténtalo cuando vuelva la red» — el BUZÓN es un concepto interno de MADRE, incomprensible para visitantes externos.
- **Por qué:** feedback `639bc1a8` (Chrome no permite pegar imágenes en textarea móvil, hint confuso) y `7ffc33cd` (visitante preguntó «¿no es suficiente con enviar el feedback aquí?» tras ver el mensaje del BUZÓN).
- **Revertir:** `git revert 23c1cbe`.

## 2026-07-04 — v0.26: descubrimientos ocultos en el mapa + enlace al desglose real
- **Qué cambió:** (1) `#mbUnlock` en el mapa — al explorar ≥4 ramas se desbloquea un aviso real (el cementerio de ideas muertas ya existía en la página; ahora se anuncia y enlaza con un botón que cierra el mapa y abre esa tarjeta); al explorar las 8 ramas se desbloquea un segundo dato: cuántos cambios reales lleva el cuaderno y desde qué fecha (leído de `window.__cmCambiosMeta`, expuesto por el IIFE de la Evolución que ya tenía ese array — cero llamadas nuevas, cero datos inventados). (2) Chip nuevo `#chipSenales` junto al "3/10" del hero, enlaza a `#senalesCard` (la sección "Señales del mundo" ya existente, con los datos NO inflables: euros, estrellas, clientes) — conecta el número resumen con su desglose real, que ya existía pero no estaba enlazado.
- **Por qué:** backlog "organismo vivo" de la crítica de GPT (ítem F "descubrimientos ocultos" + la métrica desglosada), marcados como "barato y honesto, próxima sesión, no necesita loop".
- **Verificación:** umbrales 0/3/4/6/8 con localStorage sembrado (oculto hasta 4, primer aviso en 4-7, segundo aviso en 8), botón cementerio cierra el mapa y abre la tarjeta real, chip señales abre+desplaza a `#senalesCard`, regresión completa del tap (touch+jitter) intacta, móvil 375px sin overflow, `node --check` 30/30, 0 errores consola.
- **Revertir:** `git revert <hash de esta tanda>`.

## 2026-07-04 — v0.25: abrir una rama ya no aleja el zoom (sesión paralela)
- **Qué cambió:** Tony reportó que abrir una rama con sub-ramas hacía zoom OUT si ya estaba más cerca del zoom fijo (1.5/2.1). Fix: el objetivo de zoom pasa a `Math.max(zoomFijo, zoomActual)` — entrar en una rama NUNCA reduce el zoom, solo centra.
- **Por qué:** feedback directo de Tony usando el mapa de verdad (evidencia indirecta de que v0.23 ya le dejaba tocar/reabrir ramas).
- **Verificación:** con zoom a tope (3.4), abrir otra rama mantiene 3.4 y solo centra; desde zoom bajo sigue entrando. Re-verificado por sesión posterior (4-jul) con `PointerEvent` táctiles reales.
- **Revertir:** `git revert 97b3b9f`.

## 2026-07-04 — v0.24: botón "💬 Comentar esta rama" (sesión paralela)
- **Qué cambió:** formulario inline en el panel de cada rama — crítica/sugerencia/pregunta SOBRE esa rama (no sobre la web), tipo `comentario-rama` por `window.__fb`. La rutina `cuaderno-feedback` (SKILL PASO 2) lo RUTA a un fichero nuevo del buzón para que el loop lo juzgue; NO edita el cuaderno por esto.
- **Por qué:** Tony pidió poder opinar sobre partes concretas de MADRE directamente desde el mapa.
- **Verificación:** E2E en preview (POST a `feedback` 201, payload `COMETARIO-RAMA` con rama+texto+origen); re-verificado por sesión posterior con captura del payload real.
- **Revertir:** `git revert 2d86732`.

## 2026-07-03 — v0.23: FIX definitivo del tap en móvil real (3er intento, sesión paralela) — causa REAL encontrada
- **Qué cambió:** el fix v0.21/v0.22 (pointerup+geometría) seguía fallando en el móvil REAL de Tony aunque pasaba en preview. Causa hallada con un HUD de diagnóstico en su propio navegador: el umbral tap-vs-arrastre (10px) era demasiado fino para un pulgar real (se mueve 10-20px al tocar) → se contaba como arrastre y el tap se perdía; el ratón/evento sintético se mueve 0px, por eso "pasaba" en preview. Fix: **slop 10→20** + **hit-test nativo** (`document.elementFromPoint`, con la geometría manual como red de seguridad) + **HUD opt-in `?tapdbg`** en la URL + reset de `svg.pointerEvents` al abrir el mapa.
- **Por qué:** Tony seguía reportando que no podía abrir/reabrir ramas pese a los fixes v0.19/v0.21/v0.22.
- **Verificación:** E2E con `pointerType:"touch"` — taps de 0/12/18px abren; 24px y arrastre de 200px panean sin abrir; toggle abrir↔cerrar↔reabrir; sub-rama multinivel; tap-vacío-colapsa; pinch; teclado; ratón; deep-link `#mapa`. Consola limpia. Re-verificado por sesión posterior (4-jul) con jitter de pulgar realista en viewport móvil 390×844: sigue funcionando.
- **Revertir:** `git revert 01ff0d3`. (Detalle completo con diffs y HUD: `TRASPASOS\TRASPASO_2026-07-03_mapa-tap-v023.md`.)

## 2026-07-03 — v0.22: mapa reactivo (anillos justificados + MADRE observa + huella de visitante) + captura robusta
- **Qué cambió:** aplicadas 4 mejoras de la crítica de GPT (filtradas por §9, nada fingido): (1) **anillos justificados** — cada rama muestra 2-3 líneas ↑/↓ con hechos reales de dónde sale su `benef` (campo `porque[]`; GPT: «que no parezca inventado»); (2) **MADRE observa la sesión** — `#mbExpl` da mensajes reactivos según ramas REALMENTE abiertas («te estoy viendo llegar» → «poca gente pasa de aquí» → «casi nadie llega tan lejos»); (3) **huella de visitante** — badge «🌱 nació de la propuesta de un visitante» cuando `porVisitante` (lo pone la rutina al aceptar; SKILL actualizado); (4) **promesa más fuerte** («no te prometo mejor, sí diferente»). + **Robustez del fix del tap**: ahora se captura el puntero en `pointerdown` (ya no rompe nada porque el tap va por pointerup+geometría, no por click) → el `pointerup` siempre llega aunque el dedo se levante fuera del SVG (cero punteros colgados).
- **Por qué:** Tony pegó 2 críticas de GPT (posicionamiento + «organismo vivo»); elegidas las de más valor/§9-seguras.
- **Verificación:** taps/toggle/drag/pinch/mobile-touch ✓, rangos reactivos por n (sembrando localStorage), huella con nodo de prueba, `node --check` 30/30, 0 errores. Diferido (no-fingible sin datos del sistema): deslizador de tiempo, «conversación con el pasado» por nodo, eventos aleatorios → anotados para el loop.
- **Revertir:** `git revert <hash de esta tanda>`.

## 2026-07-03 — v0.21: FIX DEFINITIVO abrir/reabrir ramas (el tap ya no depende del click del DOM)
- **Qué cambió:** el fix v0.19 (no capturar hasta arrastrar) no bastó en el navegador de Tony — seguía sin poder abrir/reabrir ramas. Rediseño de raíz: **el tap se resuelve en `pointerup`** (1 dedo, sin arrastre >10px) y se enruta al nodo por **geometría** (`nodeAtPoint`), CERO dependencia del evento `click` del DOM (que el navegador retargetea de forma impredecible con pan/zoom). El `click` dentro del SVG se neutraliza (fase captura) para que no haya doble disparo; teclado sigue por keydown; zoom/propuesta viven fuera del SVG.
- **Por qué:** Tony (3-jul): «https://…/#mapa está bugueada, no puedo abrir y reabrir ramas del mapa».
- **Verificación (ahora SÍ fiel — el mecanismo es pointerup, no el click que se enmascaraba):** batería con `PointerEvent` realistas (down/move/up): abrir→cerrar→reabrir→cerrar ✓, arrastre pana sin abrir ✓, pinch zoom ✓, tap-vacío colapsa ✓, sub-ramas ✓, modo proponer→phantom ✓, brotes ✓, deep-link #mapa ✓, teclado Enter ✓; `node --check` 30/30, 0 errores consola. Confirmación final = Tony en su móvil.
- **Revertir:** `git revert <hash de esta tanda>`.

## 2026-07-03 — v0.20: UNA historia (posicionamiento) + caos de verdad caótico
- **Qué cambió:** (1) **Cabecera = UNA historia** (crítica de posicionamiento que trajo Tony): idea única («IA que intenta mejorar sola años — ¿lo demostrará en el mundo real?») + `🔦 Hoy, de verdad` (evidencia: commits reales últimas 24 h, alimentado por el fetch del diario) + promesa de mañana + mapa 🌳 como CTA estrella. (2) **Re-secuencia runtime** (IIFE al final del body, seguro per lección v0.14): historia → hero (1 métrica) → mapa → diario → ayudar → profundidad (vs/abiertas/envivo bajan). (3) **Caos v2**: director de orquesta aleatorio (ráfagas + calmas falsas, sin intervalos fijos), 3 travesuras nuevas (lluvia de emojis, flash de color con guarda anti-overlay, tarjeta que cae y rebota), y FIX de raíz: la media query reduced-motion mataba TODAS las animaciones del caos (por eso a Tony le parecía «leve» — su navegador pide menos animaciones; mismo culpable que el hámster). El caos es OPT-IN con confirm → sus animaciones ya no se capan; lo ambiental sí se respeta.
- **Por qué:** Tony (3-jul): «el caos sigue siendo demasiado leve y poco random», «haz la experiencia más divertida/inmersiva/viva» + crítica de posicionamiento pegada («una única historia», «reduce el 60-70% inicial», «evidencia, no texto», «promesa de volver»).
- **Verificación:** orden nuevo por DOM, CTA abre mapa, 15 travesuras en 3 s, quake ya anima bajo reduce, parada limpia (0 restos), hoyReal con datos reales, móvil 375px sin overflow, `node --check` 30/30, 0 errores consola.
- **Revertir:** `git revert <hash de esta tanda>`.

## 2026-07-03 — v0.19: FIX taps del mapa (bug real desde v0.13) + hámster + beneficio por rama + brotes ⏳
- **Qué cambió (3 cosas):** (1) **BUG REAL arreglado**: `setPointerCapture` en `pointerdown` hacía que el navegador retargeteara/tragara el click → tocar una rama con dedo/ratón REAL no hacía nada (o colapsaba todo) desde v0.13; solo se abría la rama auto-abierta (por eso Tony veía «1/8» y «añadir ramas bugeado», y por eso hay CERO `propuesta-rama` en Supabase). Doble fix: captura solo con arrastre real (umbral 6px / 2 dedos) + **rescate geométrico** (un click que cae en el SVG «vacío» pero dentro de un nodo se enruta al nodo). (2) **Hámster**: bajo `prefers-reduced-motion` el cuerpo quedaba congelado (v0.15 solo giraba la rueda); reglas CSS más específicas re-activan TODA su animación. (3) **Mapa funcional**: `benef` (0-10) + `met` por rama (anillo exterior + barra en el panel + 🏆 top-3 en el pie = con qué MADRE decide qué frente empuja); propuestas → brote ⏳ «en revisión» local (localStorage, honesto) + copy con plazo real (~6 h); la rutina `cuaderno-feedback` es ahora el RECEPTOR de `propuesta-rama` (SKILL PASO 2: juzgar liberal → añadir a `__MAPA` → contarlo en Evolución; buzón avisado para que el loop NO las duplique).
- **Por qué:** Tony (3-jul): «el hámster no corre», «añadir ramas está bugeado», «métricas numéricas de beneficio para que MADRE decida lo que está funcionando».
- **Verificación:** click retargeteado simulado + camino directo + vacío + propose (4/4), brote persiste tras recarga, `node --check` 29/29, 0 errores consola. El tap 100% real lo confirma Tony en su móvil (el harness no inyecta input real — su preview_click resultó no meter eventos: cazado con control positivo).
- **Revertir:** `git revert <hash de esta tanda>`.

## 2026-07-02 — nota visible "¿no tienes salón?" en Paso 1 (feedback 0a99c3a0)
- **Qué cambió:** añadida línea `💡 ¿No tienes salón? No pasa nada — elige Opción B.` justo antes del "Responde una de dos" en la tarjeta "Tu parte / Paso 1 — frente peluquería".
- **Por qué:** visitante (id `0a99c3a0`, 2026-06-21) preguntó "no tengo ninguna peluquería qué hago". La Opción B ya lo resolvía pero dentro de un `<details>` colapsado. Llevaba parked desde 2026-06-28 (4 días → regla 1e: MADRE decide).
- **Cómo revertir:** `git revert 8d9b413` o borrar la línea `<p class="small" ...>💡 ¿No tienes salón?...</p>` en index.html.

## 2026-07-01 — chincheta más visible (feedback ffac4e41)
- **Qué cambió:** `.ns-pin` opacity subida de `.38` a `.62`; grayscale reducida de `.3` a `.1`. La chincheta del post-it (easter-egg "pa la tata") pasa de casi invisible a perceptible sin perder el toque discreto.
- **Por qué:** Tony (feedback 2026-06-28, id `ffac4e41`) pidió «haz q se vea mas claro». Llevaba parked desde 2026-06-28 (3+ días → regla 1e: MADRE decide).
- **Cómo revertir:** `git revert <hash-de-este-commit>`; o cambiar opacity a `.38` y grayscale a `.3` en `.ns-pin`.

## 2026-06-29 — v0.18: proponer rama VISUAL / por clic («rama fantasma»)
- **Qué cambió:** el botón «🌱 Proponer rama» del mapa ya no abre un formulario directo: entra en «modo proponer», tocas (o Enter/Espacio) la rama de la que nace —o el centro 🤱🏻 para una rama nueva grande—, sale una rama fantasma punteada ahí y se abre `#mbPropose` para nombrarla (el nombre se refleja inline en la fantasma, anti-XSS por `textContent`). Mismo envío `window.__fb` tipo `propuesta-rama` (contrato con el loop sin cambios). El botón por-nodo «Proponer una sub-rama aquí» sigue de base/fallback. `window.__cmVersion="v0.18"`.
- **Por qué:** Tony, tras ver v0.17, pidió que proponer fuese «clicar DÓNDE sale en el mapa, no rellenar un formulario».
- **Revisión:** workflow adversarial 5 lentes + síntesis → 3 fixes reales aplicados (fuga de `proposeMode` al salir antes de elegir; centro inalcanzable por teclado/lector; cámara metida en la rama AHORA al entrar). Verificado DOM E2E (todas las rutas) + `node --check` 29/29 + 0 errores de consola + web viva sirviendo v0.18.
- **Nota de colisión (riesgo #1):** la feature base se publicó sola en el commit `073eb95` (la rutina `cuaderno-feedback` hizo `git add -A` y la arrastró; su mensaje solo dice «refresca datos»). Los 3 fixes de review van en el commit de esta tanda.
- **Revertir:** `git revert <hash de esta tanda v0.18>` para los fixes; `git revert 073eb95` para quitar también la feature base (ojo: 073eb95 incluía un refresco de datos del loop).

## 2026-06-29 — Glosario más claro (3 entradas)
- **Qué cambió:** texto de «Suelo externo» (más directo, sin "la promesa"), «NO-RUINA» (añadida nota «Importa:» que faltaba igual que al resto de entradas), «Temperatura» (analogía maleta en el texto principal, no solo en el popup).
- **Por qué:** 6 feedbacks `tipo:simplificar` del Glosario el 2026-06-28 (IDs: 098cd136, 98356813, 3abc2651, 1cc1a420, aca2595c, db256f88). Los otros 3 (Activación, Meta-palanca, Colisión multi-IA) ya eran claros → ya-resuelto.
- **Revertir:** `git revert 9ee45de`

## 🏷️ Versiones (resumen — para saber sobre qué versión va cada idea)
> La versión actual también se ve en la web (pie de página) y en `window.__cmVersion`.
- **v0.1** · base: modo lectura público + refresco de números (TSV horneado).
- **v0.2** · comentarios públicos en vivo (Supabase) + respuesta de MADRE.
- **v0.3** · UX v2: Señales del mundo, hipótesis vivas/muertas, variedad visual, fotos en comentarios.
- **v0.4** · cuaderno vivo: gato, dibujos, tour, nota secreta, lápiz, corazón + 1ª versión del modo caótico.
- **v0.5** · HUB de FAB ✦ + ASMR (sintetizado) + "🤔 no entiendo" + caos x2 + 👁️ visitas + panel "por dentro".
- **v0.6** · texturas ASMR entre paneles + corazones 💗 persistentes + ruta guiada fuera y palpitante + caos rodeando cosas reales (sin círculos sueltos).
- **v0.7** · *(2026-06-22)* ASMR con **sonido nuevo + 8 texturas que rotan** · **plantillas de dibujo** traviesas · "no entiendo → **señalar la parte**" · caos con **clickbait + datos/órdenes random** · **etiquetas de emoji** · **skins de panel** (variedad por día) · **comentarios en burbujas** + mini-avatar · **recorte de imagen** al subir · nueva **GUIA_FACIL.md** (separa Cuaderno de MADRE).
- **v0.17** · *(actual; 2026-06-29)* **SUGERIR ramas/sub-ramas desde el mapa (lo pidió Tony) + fix del «no entiendo»**: el 🌳 mapa pasa de mirar a CO-CONSTRUIR — botón **«🌱 Proponer una sub-rama aquí»** por nodo (deduce el padre del nodo seleccionado: `parent`/`parent-path`/`parent-emoji`/`depth-padre`) + **«🌱 Proponer rama»** global en `.mbBar` (rama nueva de raíz). Abre un **panel PROPIO dentro del overlay** (`#mbPropose`, no el modal externo —que quedaba detrás del mapa por z-index 50 + bgInert—): 1 campo obligatorio (nombre) + 1 opcional (qué-es) + chip de contexto, aviso honesto «no aparece al momento; MADRE lo JUZGA». Envía por **`window.__fb`** (tabla privada `feedback`) con `tipo:"propuesta-rama"` + `texto` estructurado clave:valor que el loop parsea (nota al buzón `DESDE_CLAUDE_2026-06-29_loop-procesa-propuesta-rama-del-mapa.txt`). **Fix del bug v0.15:** el botón **«🤔 No entiendo esto»** abría el modal de feedback DETRÁS del mapa (invisible) → ahora es **un toque** vía `window.__fb` con confirmación inline. Self-contained: **Esc cierra solo el panel** (captura + `stopPropagation`), atrapa-foco propio, `svg.pointerEvents=none` mientras escribes (restaurado al cerrar y al cerrar el mapa), `toast` subido a z-index 100050. **Diseño por panel de jueces** (3 enfoques→síntesis) y **revisado por workflow adversarial de 5 lentes (10 hallazgos reales corregidos):** saneo de TODOS los campos de contexto (un `:`/salto de línea no rompe el parser del loop), `__fb` ahora devuelve `"sent"`/`"copied"` para **no mentir** si solo se copió al portapapeles (§9), `aria-modal` duplicado quitado del panel, `.mbBarBtn` a 44px táctil, nombre exige ≥2 caracteres con sentido, `closeMap` cierra el panel (sin estado sucio), `aria-label` contextual, `parent:"(raíz)"` en rama-nueva. Verificado por DOM (sub-rama de 3er nivel: `depth-padre:1`, ruta saneada, rama-nueva, regex, rama honesta «copied», cierre, móvil 375px 44px/16px sin desbordes), **0 errores/0 warnings**, `node --check` 29/29. Revertir: `git revert` del commit v0.17.
- **v0.16** · *(2026-06-29, sesión paralela)* **«MADRE te dice» v2** (canal MADRE→Tony en el cuaderno, ordenado por impacto + consecuencias permanentes). *(Hecho por otra sesión; lo respeto e integro mi v0.17 encima sin tocarlo.)*
- **v0.15** · *(2026-06-29)* **EL MAPA da otro salto (es el NÚCLEO, prioridad de Tony) + bugs/pulido**: el 🌳 mapa a pantalla completa sube de nivel — **(1) zoom-on-click**: al tocar una rama la **cámara ENTRA** (animada con `setInterval`; SALTA sin animar si reduced-motion) y se abren sus sub-ramas; **(2) descubrimiento**: al abrir el overlay se **auto-despliega la rama AHORA** (📓) + badge **«＋ N dentro»** en cada nodo con sub-ramas → soluciona el «Tony no ve las sub-ramas» (eran DESCUBRIMIENTO, ya existían); **(3) MULTINIVEL recursivo**: sub-ramas con sus propias sub-ramas (3er nivel real en 🧠KERNEL y 💈Peluquerías; el motor soporta profundidad arbitraria, horneable por el loop); **(4) «cómo avanza MADRE»** por rama (`prog`: qué hace / qué le falta) + botón **«🤔 No entiendo esto»** por nodo (reusa `window.__openFb`); **(5) progreso de exploración** `#mbExpl` («has abierto N/8 ramas», persiste en `localStorage cm_map_explored_v1`, recompensa honesta al 8/8) = razón para volver. **Bugs que vio Tony:** 🐹 **hámster vuelve a girar** aunque el navegador tenga animaciones OFF (giro lento por JS solo bajo `prefers-reduced-motion`, como un spinner; el CSS sigue para motion normal); **«pegado al margen»** arreglado (las tarjetas de contenido directo `#vsCard/#abiertasCard/#mapaCard` no tenían padding interno → su texto quedaba a 14px del borde; +aire en `.wrap` móvil); **`#envivo` menos apretado** (más padding/ritmo + «📍 AHORA» en su propio chip); **copy «ayudar a MADRE»** deja claro que **NO son deberes** (MADRE ya las busca sola). Verificado en navegador por DOM (auto-expand 4 subs, multinivel 4+3, zoom 1.5/2.1, 8/8 → 🏆, hámster avanza, padding 14→32px), **0 errores/0 warnings**, `node --check` 28/28. Revertir: `git revert` del commit v0.15.
- **v0.14** · *(2026-06-29)* **De web-viva a ORGANISMO que respira (paso 1) + 1ª pantalla re-secuenciada + bugfix del mapa**: (1) **🫀 Diario vivo** (`#diarioVivo`, tras `#envivo`): el cuaderno «respira» con cambios **REALES** — lee los **commits del repo** por la API pública de GitHub y muestra los últimos 5 con su **hora real** («hace 20 min…») + enlace a cada commit; se auto-refresca cada 3 min (pausa si la pestaña no se ve) y marca lo nuevo con «✨ nuevo». **«🌙 Desde tu última visita: N cambios nuevos»** vía `localStorage` (`cm_lastvisit_v1`) = razón honesta para volver. **Honestidad §9:** son cambios de verdad (commits), no tiempo real fingido ni autonomía inventada; **degrada en silencio** (el bloque queda oculto si la API falla / no hay red / hay límite de cuota). (2) **1ª pantalla re-secuenciada** (crítica «historia, no documentación» de Tony): primero **qué es** (`#vsCard` sube sobre el 3/10) y **por qué importa** (`#abiertasCard` sube), gancho de entrada en **1ª persona** + pregunta plantada + menos texto; el **🌳 mapa grande baja** debajo de «Camino al 10/10» (es **recompensa**, no introducción). (3) **🐞 Bugfix del mapa a pantalla completa**: en ventanas BAJAS/estrechas el bloque inferior aplastaba el mapa, los botones de zoom se salían y el pie se cortaba → ahora el bloque inferior se **acota y scrollea** (`.mbBottom`, `max-height:42vh`) y en pantallas cortas la barra es compacta y el zoom va en **fila** (verificado a 240/300/430/812px de alto y 667×360). 0 errores de consola; revisado por workflow adversarial (5 lentes). Revertir: `git revert` del commit v0.14.
- **v0.13** · *(2026-06-29)* **El mapa de MADRE → experiencia + «ventana viva»**: el 🌳 mapa de ramas gana una **vista a pantalla completa inmersiva** (deep-link `#mapa`, compartible) con **zoom/pan** sin librerías (rueda + arrastre + pellizco táctil + botones), **sub-ramas al tocar** (árbol de 2 niveles con lo que hay dentro de cada parte de MADRE), la rama **AHORA** resaltada sola, fondo oscuro ambiente, todo enmarcado como **foto** (sin fingir tiempo real) y apagable (Esc/✕). Datos del mapa movidos a `window.__MAPA` (fuente única tarjeta+overlay, **horneable por el loop** desde un futuro `SISTEMA/MAPA_RAMAS.tsv`). **`#envivo` abre con «📍 AHORA»** (foco del día como foto + «última señal vuestra» en vivo de Supabase, que se refresca) + **«🌙 Mientras no mirabas»** (3 líneas honestas, rotan por día) + **frase citable** del día. Revisado por **workflow adversarial multi-dimensión** (6 lentes, 12 hallazgos verificados); arreglados los reales: focus-trap + fondo `inert` en el overlay (cumplir aria-modal), **contraste AA** del badge de estado (texto oscuro sobre verde/ámbar/gris), `role=group` en los SVG con nodos botón, `:focus-visible` claro, botón cerrar a 44px, deep-link que ya **no saca de la página** al cerrar, y guarda anti-crash si el loop hornea un estado desconocido. 0 errores de consola; móvil 375px sin desbordes. Revertir: `git revert` del commit v0.13.
- **v0.12** · *(2026-06-29 afinado por oído de Tony + 2 tarjetas «ventana viva»)* **ASMR afinado + clarity/tensión narrativa**: tras escuchar Tony — burbujas reescrito a **pop con tono** (sonaba a madera), madera a **raspado cálido marrón+lowpass** (= madera, confirmado de oído), roce **suave vs fuerte** mucho más distintos, y golpes raros arreglados: arena=**puff granular** (fuera el thud), hojas=**crujido más denso**, pana=**«brrrp» de tela SIN el sawtooth** sintético. Niveles re-medidos offline (madera/pana salían casi mudos con bandpass resonante → recibo cálido). Página de audición con **feedback por textura + «copiar para Claude»** + acceso directo en el escritorio. **2 tarjetas nuevas**: «⚖️ Una IA normal vs MADRE» (antes/después, se entiende en 10s) y «❓ Lo que aún no sé» (preguntas abiertas reales = razón para volver) con voz en 1ª persona + humor honesto. Crítica grande «ventana viva» de Tony persistida en DESARROLLO §STAGED v0.13+. **Añadido 29-jun (2º oído):** **🌳 «El mapa de MADRE» v1** (`#mapaCard`, mapa de ramas radial inmersivo: centro+8 ramas con estado por color, clic→info, halo/flotación, apagable) + **ASMR ronda 3** (papel cracks irregulares, hojas crunchy, arena «shhh» suave con `atk`, pana ya no «pedo» = blanco mid, cremallera `buzz()` continuo/smooth) + regla **«inmersivo por defecto»** (`DISEÑO_HEURISTICAS.md` §10).
- **v0.12** · *(2026-06-28)* **ASMR por textura (modelo físico)**: reescrito el motor de sonido — cada textura tiene su síntesis propia (granos de ruido filtrado para papel/hojas/arena/pana/cremallera, **síntesis modal** —parciales que decaen— para madera/muelle, **glide de tono** para el pop de burbujas) en vez de un ruido genérico que sonaba «raro». Niveles equilibrados por **medición offline** (pico/RMS por textura, sin clipping/NaN); la síntesis es ahora la **primaria en las 8** (`PROF2SAMPLE={}`; samples CC0 siguen vivos para A/B). Página de **audición** `sounds/asmr-test.html` (iframe→motor real, sin duplicar código) para validar por oído. Revisado por **workflow adversarial** (1 fix real aplicado: Q del papel bajada para que cruja seco en vez de silbar; cap de nodos/tick en cremallera/pana para batería móvil). **Pendiente: validación por oído de Tony.** Revertir: `git revert` del commit v0.12.
- **v0.11** · *(2026-06-28)* **cabecera limpia + «no entiendo» universal + MADRE en directo**: las 4 cajas amarillas de arriba → un solo onboarding (`#onboard`) con tiempo de lectura + selector `#quickPick`; «Versión web» eliminada; marcador renombrado **«Contacto con la realidad»** con contexto ANTES; «Soy Tony»→«👤 Soy el creador»; aviso de datos viejos calmado (📸). **«No entiendo» por palabra** en el glosario (respuesta instantánea simple/ejemplo + señal deduplicada) + ramas en las tarjetas; TEMPERATURA explicada. **`#envivo` «MADRE en directo»**: latido + hámster VISIBLE + estado de ánimo del día (rota, regla de no-rayada) + «lo que no te cuento». **Cementerio de ideas muertas**. +9 plantillas de dibujo (objetos) + frase no-NPC. Caos sin temblor constante (fuera `chaosBig`) + etiquetas «mira ESTO». Formulario de comentarios restilado. Brillo «secreto» en gato/chincheta. Contenido redactado con un workflow de 6 agentes y **curado a mano** (descarté cifras inventadas; corregí «suelo externo»).
- **v0.10** · *(2026-06-28)* **ayudar + sonidos reales + frescura visible**: sección «🙌 ¿Quieres ayudar a MADRE?» (island `#ayudaBaked`, lo mantiene el loop) con cosas genéricas que le vendrían bien que alguien busque; **sonidos ASMR reales CC0** (`sounds/`: pop/wood/click, Kenney + Freesound CC0) en toques/pops con fallback al sintetizado; **hámster 🐹** (Uiverse/Nawsome, CC0-ish ref) pequeño en Rutinas; **tira «De un vistazo» pinta la frescura** (fecha + chip ROJO «posiblemente desfasado» si >48h). Bake de la rutina extendido a `AYUDA.tsv`. Raíz de la «foto vieja»: el SOURCE `CUADERNO_ESTADO.tsv` lleva 6 días sin que el loop lo refresque (no es bug de horneado; el bake es simétrico).
- **v0.9** · *(2026-06-28)* **claridad + dos públicos**: frase-gancho ("experimento público para ver si una IA mejora sola sin engañarse"), explicación en capas, glosario con "suelo externo"/"publicar 1 activo" en simple, tarjetas nuevas (Historia / Rutinas / Meta-palancas), jerarquía visual (`.key`/`.muted`/`.tony`), **separación visitante↔Tony** (las tarjetas de Tony se mueven al final con separador, sus chips se ocultan a visitantes), bugs corazón/chincheta + "¿por qué?" + "Tu parte" gateada, aviso de estado reescrito (calmado/honesto). Nota al loop para refrescar `CUADERNO_ESTADO.tsv`.
- **v0.8** · *(2026-06-28)* **modo caótico más bestia** (temblor de toda la pantalla + sacudones + tinte de color de fondo + más partículas y más rápido; todo apagable y respeta `reduced-motion`) · **lo ENCENDIDO sale fuera del menú ✦** como chip siempre visible (se ve y se apaga de un toque) · **arreglo dibujos**: ya no se puede reenviar el mismo dibujo con Ctrl+Z, ni enviar el lienzo en blanco.

---

- 2026-06-29 · **v0.16 — «MADRE te dice» v2 (feedback de Tony: menos dashboard, más consecuencias)**: el panel `#madreHabla` se reordena por IMPACTO (titular «⚡ hoy el sistema es diferente» en 3 líneas → cambios permanentes → decisiones → trabajo → conocimiento → métrica) en vez de cronología; cada cambio dice su CONSECUENCIA permanente («Aprendido X → Desde ahora Y», no «arreglé algo»); las ideas absorbidas llevan **impacto + estado + `sustituye`** (Meta Ads = en TENSIÓN con la vía orgánica de Tony; Drop-to-Brand = CONFIRMA su núcleo → ambas **ARCHIVADAS como conocimiento, no dirección**); **métrica de evolución** (281 conocidas · 3 aplicadas · 278 pendientes). Tabla `madre_para_tony` +6 columnas (migración `madre_para_tony_v2_consecuencias`). Decisión «móvil solo-texto» resuelta por MADRE (no requería criterio de Tony). Test de regresión del bug del enlace GitHub añadido (`VIDEO_PIPELINE/tests/test_twitter_y_churro.py`, 6/6 verde). Verificado en navegador (titular 3, cambios 3 con consecuencia, 1 decisión, métrica 281/3/278), 0 errores. Revertir: `git revert <hash>`.
- 2026-06-29 · **v0.15 — «MADRE te dice» (canal MADRE→Tony en el cuaderno)** (petición de Tony: «que MADRE me hable por el cuaderno, que es lo que más miro»): nuevo panel `#madreHabla` (tras `#diarioVivo`) que lee la tabla Supabase `madre_para_tony` (la escribe la rutina `cuaderno-feedback` con service_role; anon SOLO lee, RLS `for select using(hidden=false)`). Muestra **avances** (públicos), **oportunidades** de negocio absorbidas de los vídeos (públicas) y **decisiones que requieren a Tony** (solo en «modo creador» `localStorage cuaderno_madre_tony==="1"`; a visitantes solo el conteo, tras candado). Cada decisión pendiente lleva **recomendación + fecha límite + «si no decides, MADRE elige la recomendada y te avisa»** (instrumenta la regla anti-bloqueo 1e: nada se queda parado en Tony); clic en una opción → `window.__fb` (feedback) para que la rutina la recoja. `publico=false` = oculto en la UI, **NO cifrado** (lo secreto de verdad sigue yendo al buzón privado). Verificado en navegador: vista pública (2 avances + 2 oport + candado de decisiones) y vista creador (3 decisiones, 1 ✅ decidida + 2 con fecha límite y 5 botones, deadline «2 de julio»), **0 errores de consola**. Backend: migración `create_madre_para_tony`. Revertir: `git revert <hash>` (+ `drop table public.madre_para_tony` para borrar el backend).
- 2026-06-28 · **v0.11 — cabecera limpia + «no entiendo» universal + MADRE en directo + más vida** (tanda larga de feedback de Tony; commit local, SIN push hasta su OK): **(1) Cabecera (su Top 5):** 4 cajas amarillas apiladas → un solo `#onboard` (gancho + narrativa 3 frases + ⏱ tiempo de lectura + selector `#quickPick`); «Versión web» ELIMINADA; marcador → **«Contacto con la realidad»** con `.heroLead` de contexto ANTES del 3/10; «Soy Tony»→**«👤 Soy el creador»**; staleBanner calmado (📸); `caminoCard` a `.key` (3 niveles). **(2) «No entiendo» universal:** pastilla 🤔 por CADA término del glosario (data-island curado term→{simple,ejemplo}, respuesta AL INSTANTE + señal `__fb` deduplicada/sesión) + ramas simplifícalo/ejemplo en `popFor`; **TEMPERATURA** explicada (maleta) en decisión 5, paso 3 y glosario. **(3) `#envivo` «MADRE en directo»:** latido + **hámster VISIBLE** (antes oculto en card colapsada) + ánimo del día (rota por día = no-rayada) + «lo que no te cuento». Honesto: las rutinas viven aunque los números sean una foto. **(4) Cementerio de ideas muertas** (6 entradas reales). **(5) Dibujo:** +9 plantillas de objetos (cohete/taza/planta…) a 300×180 + frase no-NPC. **(6) Caos:** fuera `chaosBig` (temblor constante artificial), `jolt` espaciado, +órdenes, etiquetas «mira ESTO» en `circleReal`. **(7) Comentarios:** `.cmtForm` restilado. **(8) Brillo secreto** `.secretGlow` en gato/chincheta. Copy vía workflow de 6 agentes **curado a mano** (descartadas cifras inventadas «730 trozos»/«19 jul»; corregido «suelo externo»). Verificado: **0 errores y 0 warnings**, DOM OK. Rutina `cuaderno-feedback` PAUSADA durante la edición (anti-colisión). · revertir: `git revert <hash>`
- 2026-06-28 · **v0.10 — ayudar + sonidos reales + frescura visible** (petición de Tony; commit local, SIN push hasta su OK): (1) **Sección «🙌 ¿Quieres ayudar a MADRE?»**: island `#ayudaBaked` (tabs, seed de 5 items genéricos y moat-safe) + card + render; la rutina `cuaderno-feedback` ahora hornea también `AYUDA.tsv` (PASO 0.5). (2) **Sonidos ASMR reales**: `sounds/pop.mp3` (Freesound "Plop!" CC0), `sounds/wood.wav` + `sounds/click.wav` (Kenney Interface Sounds CC0) + `sounds/CREDITS.txt`; el motor carga y reproduce el sample real en `hit()`/`softClick()` con **fallback al sintetizado**; el roce (hover) sigue sintetizado. Verificado: los 3 ficheros cargan (200) y **decodean** (pop 0.24s / wood 0.29s / click 0.11s) — NO verificable de oído desde Code (Tony confirma). (3) **Hámster** 🐹 (Uiverse/Nawsome) pequeño (72px desktop / 60px móvil) en la tarjeta Rutinas; respeta `reduced-motion`. (4) **BUG frescura "De un vistazo"**: ahora pinta la fecha `actualizado` + chip ROJO «posiblemente desfasado» si >48h. **Causa raíz** de la foto vieja: el SOURCE `CUADERNO_ESTADO.tsv` sigue a 22-jun (el loop no lo refresca hace 6 días); el horneado es SIMÉTRICO (PASO 0.5 hornea estado+camino+ayuda) — no era bug de horneado. Verificado en navegador (1280px + móvil 375px, sin overflow, 0 errores). · revertir: `git revert <hash>`
- 2026-06-28 · **v0.9 — claridad + separar públicos** (petición de Tony + crítica externa, vía workflow de research): gancho único arriba (`#promesa`) + cue de "dos públicos" (`.sub2`); explicación en capas (subtítulos de calle + detalle en `<details>`); glosario reescrito en simple, AÑADE "suelo externo" y "publicar 1 activo" con "Importa:" (quitado "overlay"); explicador "¿qué es que MADRE necesita 2 cosas de ti?" en Señales; tarjetas nuevas 📖 Historia / ⚙️ Rutinas / 🔁 Meta-palancas; jerarquía `.card.key`/`.muted`/`.tony`; **separar públicos** (JS mueve `.card.tony` al final bajo `.tonySep`, chips de Tony ocultos a visitantes); peluquería = EN MARCHA (no pendiente); aviso desfasado → "foto de hace ~N días"; "wow" de la barra al cargar. Verificado en navegador (1280px + móvil 375px, sin overflow), 0 errores, gate de Tony intacto. Nota al loop: `DESDE_CLAUDE_2026-06-28_refresco-estado-cuaderno.txt`. · revertir: `git revert <hash>`
- 2026-06-28 · **Camino al 10/10** (plan v3 de Tony): nueva sección que lee `SISTEMA/CAMINO_10.tsv` (horneado en `#caminoBaked`, tabs reales) y pinta los huecos honestos (qué falta / qué hace MADRE / si se cierra), tras "Señales del mundo". + tira **"De un vistazo"** arriba (score · hueco principal · desviación tuya). La rutina `cuaderno-feedback` ahora hornea también `CAMINO_10.tsv` cada pasada (PASO 0.5). Verificado en navegador (desktop+móvil), 0 errores. · revertir: `git revert <hash>`
- 2026-06-28 · **v0.8** (petición de Tony, esta sesión): (1) **bug dibujos**: tras enviar un dibujo, Ctrl+Z lo recuperaba y se podía REENVIAR el mismo una y otra vez → ahora al enviar se vacía la pila de deshacer y se bloquea enviar el lienzo en blanco (`isBlank`). (2) **modo caótico más bestia** (id `3815c20d`): temblor continuo del contenido (`chaosQuake` sobre `.wrap`, solo desktop), **sacudones** puntuales (`chaosJolt`), **tinte de color** de fondo que muta (`#chaosTint`), wiggle de tarjetas más fuerte, y más partículas/más rápido. Todo apagable + respeta `prefers-reduced-motion`. (3) **HUB ✦**: lo ENCENDIDO (lápiz/corazón/caos/ASMR) ahora sale FUERA del menú como chip SIEMPRE visible (`#fabHubActive`), apagable de un toque; se sincroniza aunque salgas con Esc (MutationObserver). · verificado en navegador (1280px), 0 errores de consola · revertir: `git revert HEAD`
- 2026-06-21 · El repo `cuaderno-madre` pasa a ser la FUENTE ÚNICA DE VERDAD del cuaderno (sembrado con la última versión del PC) · base inicial · revertir: `git revert 0a2b75b`
- 2026-06-21 · **Bloque A**: modo lectura público + desbloqueo de Tony (contraseña `Jabado`, client-side); post-it explicando "feedback"; banner web corregido; y **refresco automático de números** (snapshot de `CUADERNO_ESTADO.tsv` horneado en el HTML para que la web sin Drive no muestre datos viejos) · por: petición de Tony (capa social Bloque A + "que no queden números viejos") · revertir: `git revert 8d7c729`
- 2026-06-21 · **Retoques A** (petición de Tony): la contraseña ahora SOLO protege las Decisiones; el feedback queda abierto a todos. El tipo de feedback pasa de desplegable a **botones pulsables debajo del mensaje**, con 🪄 "que elija MADRE" (`auto`) por defecto → la rutina clasifica el tipo sola. · revertir: `git revert 97935b3`
- 2026-06-21 · **Bloque B**: comentarios públicos en vivo con **Supabase** (proyecto `kopegamcjozrvmxruwdn`). Sección al final + botón flotante con panel. Comentar sin login (nombre opcional), 👍 likes (RPC atómico), hilos (responder), tiempo real. Texto escapado (anti-XSS). Tabla `public.comments` con RLS. SEPARADO del feedback privado a MADRE. · revertir: `git revert e5d0759` (y `drop table public.comments` si quieres borrar también el backend)
- 2026-06-21 · **v2 UX**: 🌍 Señales del mundo (evidencia arriba), hipótesis vivas/muertas, progreso de builds, 🤱🏻+tooltip+¿qué es?, emojis en secciones, explicación peluquería, variedad visual (tema/fondo aleatorio por visita), chat translúcido con burbujas + placeholders graciosos. · revertir: `git revert cd33d2c`
- 2026-06-21 · **Fase 2**: MADRE responde a los comentarios públicos (rutina cuaderno-feedback, columna `ai_replied`, respuestas breves/amables firmadas "MADRE 🤱🏻", ignora trolls). Respuestas de MADRE resaltadas en la UI. Keep-alive + backup de Supabase ya activos.
- 2026-06-21 · **v3**: imágenes en comentarios (Supabase Storage), corazón 💗 modo "me gusta" (perfil de gustos), ruta de avance con slider, glosario "¿pa qué sirve?", degradados + fondo variable, chat sin fondo (burbujas). Moderación: bloqueo duro de variantes de "pedófilo" + la rutina oculta amenazas fuertes contra Tony. "feedback"→"tu opinión". · revertir: `git revert 701e47f`
- 2026-06-21 · **(b) + retoques**: opinar con IMAGEN desde el móvil (tabla `public.feedback` + Storage; llega solo a MADRE, sin pegar). Flechas de ayuda ahora pegadas a su sitio y crecen al hover (antes flotaban). · revertir: `git revert 4fed3e1`
- 2026-06-21 · **Cuaderno vivo (5 widgets)**: gato easter-egg (huye al tocarlo), zona de DIBUJOS (canvas → `public.drawings` + Storage + likes/voto), tour guiado + bienvenida "¿qué es esto?", nota secreta "pa la tata :)" (post-it como papel con cinta), modo lápiz ✏️ (comentar cualquier zona, estilo Notion). Chat tipo conversación (recientes abajo, antiguos difuminados, avión al enviar). Curiosidad "¿por qué MADRE?" + provenance de los números. Heart selecciona cualquier zona. Diseñados en paralelo (workflow) + revisados (anti-XSS) + verificados en navegador.
- 2026-06-21 · **Fixes + más diversión**: arreglado tour (ya no deja la zona borrosa: backdrop sin blur), nota secreta (quitado `mask-composite` frágil que la veía rara), lápiz y corazón (ahora seleccionan cualquier zona incl. cabecera/footer; barra del lápiz pasa-clics). Añadido: **modo caótico** 🤪 (confeti, con confirmación), gato con frases troll/gamer + barriguita, **contraseña que se ríe** (🤣 al fallar "Soy Tony"), paneles que reaccionan al ratón + caret que late invitando a abrir. Nuevo `DESARROLLO_CUADERNO.md` (lecciones+inspiración) que se lee antes de editar.
- 2026-06-21 · **🎛️ HUB de FAB + ASMR + "no entiendo" + caos x2 + 6 bugs**: los 6 botones flotantes consolidados en un menú ✦ (con nudge "toca aquí"); zonas **ASMR** (Web Audio sintetizado, toggle en el hub); botones **🤔 "no entiendo"** por tarjeta y en la ruta guiada (aburre/me gusta/desarrollar) → feedback de dónde se entiende menos; **gato** con poses + arreglo móvil (touchstart passive, no atasca) + pausa por visibilitychange; **modo caótico x2** (flechas que apuntan a elementos reales, círculos, citas de comentarios reales, travesuras contextuales, megaFlip); chat abre **en lo último + foco**; comentarios in-page con **scroll interno**; post-it **más claro** (chincheta/borde); curiosidad **"MADRE = carpeta con identidad"**; **dibujos con dueño** (identidad anónima + badge "tú"); **modos lápiz/corazón exclusivos** (no se solapan barras). Bugs ChatGPT: chips de cabecera **reales** (eran 4, son 5), aviso **estado desfasado >48h**, `.limit()` en comments/drawings, **validación+resize de imagen**, tema **por día** (huella visual estable), Open Graph, `aria-live` en toasts. Nuevo `DISEÑO_HEURISTICAS.md`. Verificado en navegador + review adversarial (workflow). · revertir: `git revert HEAD`
- 2026-06-21 · **👁️ Visitas + panel + caos con chicha + 2 bugs**: contador de visitas (Supabase: tabla `counters` + RPC `bump_counter` SECURITY DEFINER; 1×/sesión; empieza en 0) que abre el panel **"El cuaderno por dentro"** (comentarios/dibujo más votados, actividad, "lo que opina MADRE" rotatorio). **Modo caótico narrativo**: flechas que señalan + notas y avisos absurdos (no solo confeti) + recorte ~70% en móvil. **Bugs**: gato ya no se tapa con la barra superior (`TOPSAFE`/safe-area); **deshacer** en dibujos (botón ↩️ Atrás + Ctrl/Cmd+Z). Nuevo `MADRE_UI_REFERENCIAS.md` (favoritos de Uiverse de Tony) y backlog "organismo vivo" persistido en `DESARROLLO_CUADERNO.md`. Verificado en navegador (móvil), 0 errores. · revertir: `git revert HEAD`
- 2026-06-22 · **v0.7.1 — fix plantilla de dibujo**: la plantilla dejaba "resto" en el dibujo enviado porque se horneaba en el lienzo. Ahora es una **GUÍA superpuesta** (`#dbjsGuide`, `<img>` aparte, `pointer-events:none`); el lienzo es **transparente** (el blanco lo pone el marco) y al enviar se **compone blanco + trazos** (sin guía). La plantilla solo te guía al dibujar; no sale en el resultado. · revertir: `git revert HEAD`
- 2026-06-22 · **v0.7 — ASMR con sonido nuevo + variedad + plantillas de dibujo + "señalar lo que no entiendo" + clickbait/datos en caos + emoji labels + skins + comentarios en burbujas + recorte de imagen + GUIA_FACIL** (petición de Tony): (1) ASMR motor reescrito (`nburst`/`blip` + `brush(prof,speed)` + `hit(prof)`, suena más real) y **8 texturas distintas** (`burbujas/pana/arena/papel/madera/hojas/muelle/cremallera`) que **rotan por carga sin repetir** en la misma página. (2) **Plantillas de dibujo** (24, generadas por workflow + validadas) en `window.__cmCorpus`: `#dbjsTplToggle` → strip de chips → `applyTpl` rasteriza SVG line-art al canvas para calcar; nudge al bajar a la sección (IntersectionObserver). (3) **"🤔 no entiendo" → señalar la parte** concreta (modo `nieSelect`, clic en captura) **o solo seguir**. (4) Caos: flechas **clickbait**, **datos super-random + órdenes/retos + más comentarios** del corpus (`spawnFact`). (5) Tour: **paso sorpresa** = zona de dibujos. (6) **Etiquetas de emoji** (title/aria en botones solo-emoji). (7) **Skins de panel** (8, estables por día → variedad visual). (8) **Comentarios en burbujas** + mini-avatar. (9) **Recorte de imagen** al subir (`cropImg`, modal arrastrable; usar entera/cancelar). (10) HUB: lo encendido sube arriba del menú. (11) **Versión visible** (v0.7, pie) + resumen de versiones aquí + nueva **`GUIA_FACIL.md`** (separa Cuaderno↔MADRE, explica lo mejor de cada uno en simple). Contenido generado con un workflow de 8 agentes; diff revisado por workflow adversarial; verificado en navegador (0 errores). · revertir: `git revert HEAD`
- 2026-06-22 · **🤲 ASMR-texturas + 💗 corazones persistentes + 🧭 ruta guiada fuera/palpitante + caos sin círculos sueltos** (petición de Tony): (1) **ASMR de verdad**: texturas `<aside class="cmTex">` entre paneles (rózalas = fricción `brush(speed)` modulada por velocidad del puntero) + cositas que **revientan** (`.cmBub` burbujas → `pop()`, se rellenan solas) o **crujen/parten** (`.cmCrumb` 🍂/🥨 → `crunch()`/`crack()`); **ON por defecto** (respeta `cm_asmr_v1="0"` si el usuario lo apagó; audio se desbloquea al primer gesto). (2) **Corazón**: al marcar zona deja un 💗 fijo (capa `#likeHearts`, anclado por `cssPath` nth-of-type, reposiciona en scroll/resize) con **tu nota al hover** y "quitar 💔"; sustituye `window.prompt` por mini-popover `#likeAsk`; persiste en `cm_likes_v1`. (3) **🧭 Ruta guiada** y **👁️ El cuaderno por dentro** salen del HUB → siempre visibles a la izq.; la ruta **palpita** (`.callme` + globito "👈 empieza por aquí") al entrar hasta el primer toque. (4) **Modo caótico**: fuera los círculos de colores estáticos; ahora **rodea elementos reales** con un círculo dibujado a mano (`circleReal`, SVG ellipse con stroke-dashoffset). Verificado en navegador (0 errores; flujos probados por eval). · revertir: `git revert HEAD`
- 2026-06-28 · **📍 Tarjeta "¿dónde vive el cuaderno y cuándo te lee?" (SVG explicativo) + arreglo: ya se pueden comentar/❤️ las tiras ASMR** · por: confusión de Tony sobre dónde vive la web (sesión Claude Code) + feedback id `c3c1f7f8` ("no me deja comentar la zona del ASMR" — faltaba `.cmTex` en el selector del lápiz/corazón/cursor). Verificado en navegador a 375px (móvil): SVG 315×431 con aspecto correcto, cabe sin desbordar, `.cmTex` seleccionable, 0 errores de consola. · revertir: `git revert HEAD`
